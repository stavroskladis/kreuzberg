# This file is auto-generated by alef. DO NOT EDIT.
"""E2e tests for category: token_reduction."""
import pytest

from kreuzberg import extract_file


@pytest.mark.asyncio
async def test_token_reduction_aggressive() -> None:
    """Tests aggressive token reduction mode significantly reduces content."""
    path = ""
    result = await extract_file(path=path)
    assert result.mime_type.strip() == "application/pdf"
    assert len(result.content) >= 5
    assert len(result.content) <= 150
    assert result.content

@pytest.mark.asyncio
async def test_token_reduction_basic() -> None:
    """Tests basic token reduction on PDF document."""
    path = ""
    result = await extract_file(path=path)
    assert result.mime_type.strip() == "application/pdf"
    assert len(result.content) >= 5
    assert len(result.content) <= 200
    assert result.content

@pytest.mark.asyncio
async def test_token_reduction_light() -> None:
    """Tests light token reduction mode preserves most content."""
    path = ""
    result = await extract_file(path=path)
    assert result.mime_type.strip() == "application/pdf"
    assert len(result.content) >= 10
    assert result.content

@pytest.mark.asyncio
async def test_token_reduction_with_chunking() -> None:
    """Tests token reduction combined with chunking."""
    path = ""
    result = await extract_file(path=path)
    assert result.mime_type.strip() == "application/pdf"
    assert len(result.content) >= 5
    assert len(result.content) <= 200
    assert result.content
    assert len(result.chunks) >= 1
    # TODO: unsupported assertion type: is_true

