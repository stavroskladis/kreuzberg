// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:d6994f3fa5700a48fc3aafbb74b731ed805bebaed9be1c9f9afe5f1619f4e0c1
// To regenerate: alef generate
// To verify freshness: alef verify --exit-code
// Issues & docs: https://github.com/kreuzberg-dev/alef
using System.Text.Json;
using System.Text.Json.Serialization;
using System.Threading.Tasks;
using Xunit;
using Kreuzberg;

namespace Kreuzberg.E2e;

/// <summary>E2e tests for category: error.</summary>
public class ErrorTests
{
    private static readonly JsonSerializerOptions ConfigOptions = new() { Converters = { new JsonStringEnumConverter(JsonNamingPolicy.SnakeCaseLower) }, DefaultIgnoreCondition = JsonIgnoreCondition.WhenWritingDefault };

    [Fact]
    public void Test_ErrorEmptyBytes()
    {
        // Graceful handling of empty bytes (should not error)
        var result = Kreuzberg.ExtractBytesSync("text/empty.txt", "text/plain", null);
    }

    [Fact]
    public void Test_ErrorEmptyMime()
    {
        // Error when extracting with empty MIME type
        Assert.Throws<KreuzbergException>(() => Kreuzberg.ExtractBytesSync("text/plain.txt", "", null));
    }

    [Fact]
    public void Test_ErrorExtractBytesConflictingOcr()
    {
        // extract_bytes force+disable OCR
        Assert.Throws<KreuzbergException>(() => Kreuzberg.ExtractBytesSync("SGVsbG8=", "text/plain", new ExtractionConfig { DisableOcr = true, ForceOcr = true }));
    }

    [Fact]
    public void Test_ErrorFileNotFound()
    {
        // Error when file does not exist (sync)
        Assert.Throws<KreuzbergException>(() => Kreuzberg.ExtractFileSync("nonexistent/file.pdf", null, null));
    }

    [Fact]
    public async Task Test_ErrorFileNotFoundAsync()
    {
        // Error when file does not exist (async)
        await Assert.ThrowsAsync<KreuzbergException>(() => Kreuzberg.ExtractFile("nonexistent/file.pdf", null, null));
    }

    [Fact]
    public void Test_ErrorInvalidChunkOverlap()
    {
        // Error when chunk overlap exceeds max_characters
        Assert.Throws<KreuzbergException>(() => Kreuzberg.ExtractFileSync("pdf/fake_memo.pdf", null, new ExtractionConfig { Chunking = "{\"max_characters\":10,\"overlap_characters\":100}" }));
    }

    [Fact]
    public void Test_ErrorInvalidConfidence()
    {
        // Error when OCR confidence threshold is out of range
        Assert.Throws<KreuzbergException>(() => Kreuzberg.ExtractFileSync("pdf/fake_memo.pdf", null, new ExtractionConfig { Ocr = "{\"confidence_threshold\":2.0}" }));
    }

    [Fact]
    public void Test_ErrorInvalidDpi()
    {
        // Error when OCR DPI is invalid (negative)
        Assert.Throws<KreuzbergException>(() => Kreuzberg.ExtractFileSync("pdf/fake_memo.pdf", null, new ExtractionConfig { Ocr = "{\"dpi\":-1}" }));
    }

    [Fact]
    public void Test_ErrorInvalidMimeFormat()
    {
        // Error when extracting with invalid MIME type format
        Assert.Throws<KreuzbergException>(() => Kreuzberg.ExtractBytesSync("text/plain.txt", "not-a-mime", null));
    }

    [Fact]
    public void Test_ErrorInvalidOcrLanguage()
    {
        // Error when OCR language pack is not available
        Assert.Throws<KreuzbergException>(() => Kreuzberg.ExtractFileSync("images/balance_sheet_1.png", null, new ExtractionConfig { Ocr = "{\"force_ocr\":true,\"language\":\"zzz_nonexistent\"}" }));
    }

    [Fact]
    public void Test_ErrorPdfCorrupt()
    {
        // Error when PDF is truncated/corrupt
        Assert.Throws<KreuzbergException>(() => Kreuzberg.ExtractFileSync("pdf/corrupt_truncated.pdf", null, null));
    }

    [Fact]
    public void Test_ErrorPdfNeedsPassword()
    {
        // Error when password-protected PDF is accessed without password
        Assert.Throws<KreuzbergException>(() => Kreuzberg.ExtractFileSync("pdf/password_protected.pdf", null, null));
    }

    [Fact]
    public void Test_ErrorPdfPageOutOfRange()
    {
        // Graceful handling of out-of-range page extraction
        var result = Kreuzberg.ExtractFileSync("pdf/fake_memo.pdf", null, new ExtractionConfig { Pages = "999-1000" });
    }

    [Fact]
    public void Test_ErrorPdfWrongPassword()
    {
        // Error when PDF password is incorrect
        Assert.Throws<KreuzbergException>(() => Kreuzberg.ExtractFileSync("pdf/password_protected.pdf", null, new ExtractionConfig { Pdf = "{\"password\":\"wrongpassword\"}" }));
    }

    [Fact]
    public void Test_ErrorRenderCorruptPdf()
    {
        // Error when rendering page from corrupt PDF
        Assert.Throws<KreuzbergException>(() => Kreuzberg.RenderPage("pdf/corrupt_truncated.pdf", 0, null));
    }

    [Fact]
    public void Test_ErrorRenderInvalidPage()
    {
        // Error when rendering page number exceeds document page count
        Assert.Throws<KreuzbergException>(() => Kreuzberg.RenderPage("pdf/fake_memo.pdf", 999, null));
    }

    [Fact]
    public void Test_ErrorSecurityArchiveFiles()
    {
        // Error when archive file count exceeds security limit
        Assert.Throws<KreuzbergException>(() => Kreuzberg.ExtractFileSync("archives/documents.zip", null, new ExtractionConfig { SecurityLimits = "{\"max_files_in_archive\":0}" }));
    }

    [Fact]
    public void Test_ErrorSecurityArchiveSize()
    {
        // Error when archive size exceeds security limit
        Assert.Throws<KreuzbergException>(() => Kreuzberg.ExtractFileSync("archives/documents.zip", null, new ExtractionConfig { SecurityLimits = "{\"max_archive_size\":1}" }));
    }

    [Fact]
    public void Test_ErrorSecurityContentTooLarge()
    {
        // Error when content exceeds security limit for max size
        Assert.Throws<KreuzbergException>(() => Kreuzberg.ExtractFileSync("pdf/fake_memo.pdf", null, new ExtractionConfig { SecurityLimits = "{\"max_content_size\":10}" }));
    }

    [Fact]
    public void Test_ErrorSecurityNestingDepth()
    {
        // Error when XML nesting depth exceeds security limit
        Assert.Throws<KreuzbergException>(() => Kreuzberg.ExtractFileSync("html/complex_table.html", null, new ExtractionConfig { SecurityLimits = "{\"max_nesting_depth\":1}" }));
    }

    [Fact]
    public void Test_ErrorTimeoutExtraction()
    {
        // Error when extraction times out
        Assert.Throws<KreuzbergException>(() => Kreuzberg.ExtractFileSync("pdf/large.pdf", null, new ExtractionConfig { ExtractionTimeoutSecs = 1 }));
    }

    [Fact]
    public void Test_ErrorUnsupportedMime()
    {
        // Error when extracting with unsupported MIME type
        Assert.Throws<KreuzbergException>(() => Kreuzberg.ExtractBytesSync("text/plain.txt", "application/x-nonexistent", null));
    }
}
