// This file is auto-generated by alef. DO NOT EDIT.
import { describe, it, expect } from 'vitest';
import { extract_file } from 'kreuzberg';

describe('contract', () => {
  it('api_batch_bytes_async: Tests async batch bytes extraction API (batch_extract_bytes)', async () => {
    const result = await extract_file();
    expect(result.mimeType.trim()).toBe("application/pdf");
    // TODO: unsupported assertion type: min_length
    expect(["May 5, 2023", "Mallori"].some((v) => result.content.includes(v))).toBe(true);
  });

  it('api_batch_bytes_sync: Tests sync batch bytes extraction API (batch_extract_bytes_sync)', async () => {
    const result = await extract_file();
    expect(result.mimeType.trim()).toBe("application/pdf");
    // TODO: unsupported assertion type: min_length
    expect(["May 5, 2023", "Mallori"].some((v) => result.content.includes(v))).toBe(true);
  });

  it('api_batch_bytes_with_configs_async: Tests async batch bytes extraction with per-file configs (batch_extract_bytes with file_configs parameter)', async () => {
    const result = await extract_file();
    expect(result.mimeType.trim()).toBe("application/pdf");
    // TODO: unsupported assertion type: min_length
    expect(result.outputFormat.trim()).toBe("markdown");
  });

  it('api_batch_bytes_with_configs_sync: Tests sync batch bytes extraction with per-file configs (batch_extract_bytes_sync with file_configs parameter)', async () => {
    const result = await extract_file();
    expect(result.mimeType.trim()).toBe("application/pdf");
    // TODO: unsupported assertion type: min_length
    expect(result.outputFormat.trim()).toBe("markdown");
  });

  it('api_batch_file_async: Tests async batch file extraction API (batch_extract_file)', async () => {
    const result = await extract_file();
    expect(result.mimeType.trim()).toBe("application/pdf");
    // TODO: unsupported assertion type: min_length
    expect(["May 5, 2023", "Mallori"].some((v) => result.content.includes(v))).toBe(true);
  });

  it('api_batch_file_sync: Tests sync batch file extraction API (batch_extract_file_sync)', async () => {
    const result = await extract_file();
    expect(result.mimeType.trim()).toBe("application/pdf");
    // TODO: unsupported assertion type: min_length
    expect(["May 5, 2023", "Mallori"].some((v) => result.content.includes(v))).toBe(true);
  });

  it('api_batch_file_with_configs_async: Tests async batch file extraction with per-file configs (batch_extract_files with file_configs parameter)', async () => {
    const result = await extract_file();
    expect(result.mimeType.trim()).toBe("application/pdf");
    // TODO: unsupported assertion type: min_length
    expect(result.outputFormat.trim()).toBe("markdown");
  });

  it('api_batch_file_with_configs_sync: Tests sync batch file extraction with per-file configs (batch_extract_files_sync with file_configs parameter)', async () => {
    const result = await extract_file();
    expect(result.mimeType.trim()).toBe("application/pdf");
    // TODO: unsupported assertion type: min_length
    expect(result.outputFormat.trim()).toBe("markdown");
  });

  it('api_batch_file_with_timeout_sync: Tests sync batch file extraction with per-file timeout config override', async () => {
    const result = await extract_file();
    expect(result.mimeType.trim()).toBe("application/pdf");
    // TODO: unsupported assertion type: min_length
  });

  it('api_extract_bytes_async: Tests async bytes extraction API (extract_bytes)', async () => {
    const result = await extract_file();
    expect(result.mimeType.trim()).toBe("application/pdf");
    // TODO: unsupported assertion type: min_length
    expect(["May 5, 2023", "Mallori"].some((v) => result.content.includes(v))).toBe(true);
  });

  it('api_extract_bytes_sync: Tests sync bytes extraction API (extract_bytes_sync)', async () => {
    const result = await extract_file();
    expect(result.mimeType.trim()).toBe("application/pdf");
    // TODO: unsupported assertion type: min_length
    expect(["May 5, 2023", "Mallori"].some((v) => result.content.includes(v))).toBe(true);
  });

  it('api_extract_file_async: Tests async file extraction API (extract_file)', async () => {
    const result = await extract_file();
    expect(result.mimeType.trim()).toBe("application/pdf");
    // TODO: unsupported assertion type: min_length
    expect(["May 5, 2023", "Mallori"].some((v) => result.content.includes(v))).toBe(true);
  });

  it('api_extract_file_sync: Tests sync file extraction API (extract_file_sync)', async () => {
    const result = await extract_file();
    expect(result.mimeType.trim()).toBe("application/pdf");
    // TODO: unsupported assertion type: min_length
    expect(["May 5, 2023", "Mallori"].some((v) => result.content.includes(v))).toBe(true);
  });

  it('config_acceleration_cpu_provider: Tests explicit CPU acceleration provider configuration', async () => {
    const result = await extract_file();
    expect(result.mimeType.trim()).toBe("application/pdf");
    // TODO: unsupported assertion type: min_length
    expect(["May 5, 2023", "To Whom it May Concern"].some((v) => result.content.includes(v))).toBe(true);
  });

  it('config_chunking: Tests chunking configuration with chunk assertions', async () => {
    const result = await extract_file();
    expect(result.mimeType.trim()).toBe("application/pdf");
    // TODO: unsupported assertion type: min_length
    expect(result.chunks.length).toBeGreaterThanOrEqual(1);
    // TODO: unsupported assertion type: is_true
  });

  it('config_chunking_text: Tests text chunker type (generic whitespace/punctuation splitter)', async () => {
    const result = await extract_file();
    expect(result.mimeType.trim()).toBe("application/pdf");
    // TODO: unsupported assertion type: min_length
    expect(result.chunks.length).toBeGreaterThanOrEqual(1);
    // TODO: unsupported assertion type: is_true
  });

  it('config_disable_ocr: Tests disable_ocr configuration option - OCR is skipped for images', async () => {
    const result = await extract_file();
    expect(result.mimeType.trim()).toBe("image/png");
    // TODO: unsupported assertion type: max_length
  });

  it('config_document_structure: Tests include_document_structure config produces document tree', async () => {
    const result = await extract_file();
    expect(result.mimeType.trim()).toBe("application/pdf");
    expect(result.documentStructure.length).toBeGreaterThan(0);
    expect(result.documentStructure.nodes.length).toBeGreaterThanOrEqual(1);
    expect(result.documentStructure.nodeTypes).toContain("paragraph");
  });

  it('config_document_structure_disabled: Tests document field is null when include_document_structure is false', async () => {
    const result = await extract_file();
    expect(result.mimeType.trim()).toBe("application/pdf");
  });

  it('config_document_structure_with_headings: Tests document structure with DOCX heading-driven nesting', async () => {
    const result = await extract_file();
    expect(result.mimeType.trim()).toBe("application/vnd.openxmlformats-officedocument.wordprocessingml.document");
    expect(result.documentStructure.length).toBeGreaterThan(0);
    expect(result.documentStructure.nodes.length).toBeGreaterThanOrEqual(1);
  });

  it('config_email_msg_fallback_codepage: Tests MSG extraction with custom fallback codepage for Cyrillic', async () => {
    const result = await extract_file();
    expect(result.mimeType.trim()).toBe("application/vnd.ms-outlook");
    // TODO: unsupported assertion type: min_length
  });

  it('config_extraction_timeout: Tests that extraction_timeout_secs config field is accepted and does not affect fast extractions', async () => {
    const result = await extract_file();
    expect(result.mimeType.trim()).toBe("application/pdf");
    // TODO: unsupported assertion type: min_length
  });

  it('config_html_options: Tests extraction with HTML conversion options configured', async () => {
    const result = await extract_file();
    expect(["text/html"].some((v) => result.mimeType.includes(v))).toBe(true);
    // TODO: unsupported assertion type: min_length
    expect(result.content.length).toBeGreaterThan(0);
  });

  it('config_html_styled_custom_css: HTML output with inline user CSS string is embedded verbatim in the style block', async () => {
    const result = await extract_file();
    expect(result.outputFormat.trim()).toBe("html");
    expect(result.content).toContain(".kb-p { color: red; }");
    expect(result.content).toContain("kb-doc");
  });

  it('config_html_styled_default: HTML output with default theme emits kb-* class hooks and an embedded style block', async () => {
    const result = await extract_file();
    expect(result.mimeType.trim()).toBe("application/pdf");
    expect(result.outputFormat.trim()).toBe("html");
    expect(result.content).toContain("kb-doc");
    expect(result.content).toContain("kb-content");
    expect(result.content).toContain("kb-p");
    expect(result.content).toContain("<style>");
  });

  it('config_html_styled_no_embed: HTML output with embed_css false emits class hooks but no style block', async () => {
    const result = await extract_file();
    expect(result.outputFormat.trim()).toBe("html");
    expect(result.content).toContain("kb-doc");
    expect(result.content).toContain("kb-content");
  });

  it('config_images: Tests image extraction configuration with image assertions', async () => {
    const result = await extract_file();
    expect(result.mimeType.trim()).toBe("application/pdf");
    expect(result.images.length).toBeGreaterThanOrEqual(1);
  });

  it('config_images_with_formats: Tests image extraction on PPTX containing embedded images', async () => {
    const result = await extract_file();
    expect(result.mimeType.trim()).toBe("application/vnd.openxmlformats-officedocument.presentationml.presentation");
    expect(result.images.length).toBeGreaterThanOrEqual(1);
  });

  it('config_language_detection: Tests language detection configuration', async () => {
    const result = await extract_file();
    expect(result.mimeType.trim()).toBe("application/pdf");
    // TODO: unsupported assertion type: min_length
    expect(result.detectedLanguages).toContain("eng");
    expect(result.languageDetectionConfidence).toBeGreaterThanOrEqual(0.5);
  });

  it('config_language_detection_multi: Tests language detection with detect_multiple and min_confidence options', async () => {
    const result = await extract_file();
    expect(result.mimeType.trim()).toBe("application/pdf");
    // TODO: unsupported assertion type: min_length
    expect(result.detectedLanguages).toContain("eng");
  });

  it('config_postprocessor: Tests postprocessor config is accepted and extraction succeeds', async () => {
    const result = await extract_file();
    expect(result.mimeType.trim()).toBe("application/pdf");
    // TODO: unsupported assertion type: min_length
    expect(result.content.length).toBeGreaterThan(0);
  });

  it('config_processing_warnings_empty: Tests that a clean PDF extraction produces no processing warnings', async () => {
    const result = await extract_file();
    expect(result.mimeType.trim()).toBe("application/pdf");
    // TODO: unsupported assertion type: min_length
    expect(result.processingWarnings.trim()).toHaveLength(0);
  });

  it('config_quality_disabled: Tests extraction with quality processing explicitly disabled', async () => {
    const result = await extract_file();
    expect(result.mimeType.trim()).toBe("application/pdf");
    // TODO: unsupported assertion type: min_length
    expect(result.content.length).toBeGreaterThan(0);
  });

  it('config_security_limits: Tests archive extraction with custom security limits', async () => {
    const result = await extract_file();
    expect(["application/zip", "application/x-zip-compressed"].some((v) => result.mimeType.includes(v))).toBe(true);
    // TODO: unsupported assertion type: min_length
  });

  it('config_tables_content: Tests table extraction with content_contains_any assertion on DOCX with tables', async () => {
    const result = await extract_file();
    expect(result.mimeType.trim()).toBe("application/vnd.openxmlformats-officedocument.wordprocessingml.document");
    expect(result.tables.length).toBeGreaterThanOrEqual(1);
  });

  it('config_use_cache_false: Tests use_cache=false configuration option', async () => {
    const result = await extract_file();
    expect(result.mimeType.trim()).toBe("application/pdf");
    // TODO: unsupported assertion type: min_length
  });

  it('output_format_bytes_markdown: Tests markdown output format via bytes extraction API', async () => {
    const result = await extract_file();
    expect(result.mimeType.trim()).toBe("application/pdf");
    // TODO: unsupported assertion type: min_length
    expect(result.outputFormat.trim()).toBe("markdown");
  });

  it('output_format_djot: Tests Djot output format', async () => {
    const result = await extract_file();
    expect(result.mimeType.trim()).toBe("application/pdf");
    // TODO: unsupported assertion type: min_length
    expect(result.outputFormat.trim()).toBe("djot");
  });

  it('output_format_html: Tests HTML output format', async () => {
    const result = await extract_file();
    expect(result.mimeType.trim()).toBe("application/pdf");
    // TODO: unsupported assertion type: min_length
    expect(result.outputFormat.trim()).toBe("html");
  });

  it('output_format_markdown: Tests Markdown output format', async () => {
    const result = await extract_file();
    expect(result.mimeType.trim()).toBe("application/pdf");
    // TODO: unsupported assertion type: min_length
    expect(result.outputFormat.trim()).toBe("markdown");
  });

  it('output_format_plain: Tests Plain output format', async () => {
    const result = await extract_file();
    expect(result.mimeType.trim()).toBe("application/pdf");
    // TODO: unsupported assertion type: min_length
    expect(result.outputFormat.trim()).toBe("plain");
  });

  it('result_format_element_based: Tests ElementBased result format with element assertions', async () => {
    const result = await extract_file();
    expect(result.mimeType.trim()).toBe("application/pdf");
    expect(result.resultFormat.trim()).toBe("element_based");
    expect(result.elements.length).toBeGreaterThanOrEqual(1);
  });

  it('result_format_unified: Tests Unified result format (default)', async () => {
    const result = await extract_file();
    expect(result.mimeType.trim()).toBe("application/pdf");
    // TODO: unsupported assertion type: min_length
    expect(result.resultFormat.trim()).toBe("unified");
  });
});
