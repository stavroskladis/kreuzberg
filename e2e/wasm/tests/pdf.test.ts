// This file is auto-generated by alef. DO NOT EDIT.
import { describe, it, expect } from 'vitest';
import { extract_file } from 'kreuzberg';

describe('pdf', () => {
  it('pdf_assembly_technical: Assembly language technical manual with large body of text.', async () => {
    const result = await extract_file();
    expect(result.mimeType.trim()).toBe("application/pdf");
    // TODO: unsupported assertion type: min_length
    expect(["assembly", "register", "instruction"].some((v) => result.content.includes(v))).toBe(true);
    expect(result.metadata.formatType.trim()).toBe("pdf");
  });

  it('pdf_bayesian_data_analysis: Bayesian data analysis textbook PDF with large content volume.', async () => {
    const result = await extract_file();
    expect(result.mimeType.trim()).toBe("application/pdf");
    // TODO: unsupported assertion type: min_length
    expect(["Bayesian", "probability", "distribution"].some((v) => result.content.includes(v))).toBe(true);
    expect(result.metadata.formatType.trim()).toBe("pdf");
  });

  it('pdf_code_and_formula: PDF containing code snippets and formulas should retain substantial content.', async () => {
    const result = await extract_file();
    expect(result.mimeType.trim()).toBe("application/pdf");
    // TODO: unsupported assertion type: min_length
  });

  it('pdf_deep_learning: Deep learning textbook PDF to ensure long-form extraction quality.', async () => {
    const result = await extract_file();
    expect(result.mimeType.trim()).toBe("application/pdf");
    // TODO: unsupported assertion type: min_length
    expect(["neural", "network", "deep learning"].some((v) => result.content.includes(v))).toBe(true);
    expect(result.metadata.formatType.trim()).toBe("pdf");
  });

  it('pdf_embedded_images: PDF with embedded images should extract text and tables when present.', async () => {
    const result = await extract_file();
    expect(result.mimeType.trim()).toBe("application/pdf");
    // TODO: unsupported assertion type: min_length
    expect(result.tables.length).toBeGreaterThanOrEqual(0);
  });

  it('pdf_google_doc: Google Docs exported PDF to verify conversion fidelity.', async () => {
    const result = await extract_file();
    expect(result.mimeType.trim()).toBe("application/pdf");
    // TODO: unsupported assertion type: min_length
    expect(result.metadata.formatType.trim()).toBe("pdf");
  });

  it('pdf_large_ciml: Large machine learning textbook PDF to stress extraction length.', async () => {
    const result = await extract_file();
    expect(result.mimeType.trim()).toBe("application/pdf");
    // TODO: unsupported assertion type: min_length
    expect(["machine learning", "algorithm", "training"].some((v) => result.content.includes(v))).toBe(true);
    expect(result.metadata.formatType.trim()).toBe("pdf");
  });

  it('pdf_non_english_german: German technical PDF to ensure non-ASCII content extraction.', async () => {
    const result = await extract_file();
    expect(result.mimeType.trim()).toBe("application/pdf");
    // TODO: unsupported assertion type: min_length
    expect(["Intel", "paging"].some((v) => result.content.includes(v))).toBe(true);
    expect(result.metadata.formatType.trim()).toBe("pdf");
  });

  it('pdf_right_to_left: Right-to-left language PDF to verify RTL extraction.', async () => {
    const result = await extract_file();
    expect(result.mimeType.trim()).toBe("application/pdf");
    // TODO: unsupported assertion type: min_length
    expect(result.metadata.formatType.trim()).toBe("pdf");
  });

  it('pdf_simple_text: Simple text-heavy PDF should extract content without OCR or tables.', async () => {
    const result = await extract_file();
    expect(result.mimeType.trim()).toBe("application/pdf");
    // TODO: unsupported assertion type: min_length
    expect(["May 5, 2023", "To Whom it May Concern", "Mallori"].some((v) => result.content.includes(v))).toBe(true);
  });

  it('pdf_tables_large: Large PDF with extensive tables to stress table extraction.', async () => {
    const result = await extract_file();
    expect(result.mimeType.trim()).toBe("application/pdf");
    // TODO: unsupported assertion type: min_length
  });

  it('pdf_tables_medium: Medium-sized PDF with multiple tables.', async () => {
    const result = await extract_file();
    expect(result.mimeType.trim()).toBe("application/pdf");
    // TODO: unsupported assertion type: min_length
  });

  it('pdf_technical_stat_learning: Technical statistical learning PDF requiring substantial extraction.', async () => {
    const result = await extract_file();
    expect(result.mimeType.trim()).toBe("application/pdf");
    // TODO: unsupported assertion type: min_length
    expect(["statistical", "regression", "learning"].some((v) => result.content.includes(v))).toBe(true);
    expect(result.metadata.formatType.trim()).toBe("pdf");
  });
});
