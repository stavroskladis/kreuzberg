// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:68bf06b56e9ad2c135c1f387e945f5cd4568363b388d20ddd32f9c55eb99e469
// To regenerate: alef generate
// To verify freshness: alef verify --exit-code
// Issues & docs: https://github.com/kreuzberg-dev/alef
import { describe, it, expect } from 'vitest';
import { extract_file, serialize_to_json, serialize_to_toon, JsExtractionConfig } from 'kreuzberg';

describe('serialization', () => {
  it('serialize_json_basic: Serialize extraction result to JSON', () => {
    const options = new JsExtractionConfig();
    const result = serialize_to_json("test_documents/text/plain.txt", options);
    expect(result.content.length).toBeGreaterThanOrEqual(1);
  });

  it('serialize_json_pdf: Serialize PDF extraction to JSON', () => {
    const options = new JsExtractionConfig();
    const result = serialize_to_json("test_documents/pdf/sample.pdf", options);
    expect(result.content).toContain("content");
  });

  it('serialize_toon_basic: Serialize extraction result to TOON format', () => {
    const options = new JsExtractionConfig();
    const result = serialize_to_toon("test_documents/text/plain.txt", options);
    expect(result.content.length).toBeGreaterThanOrEqual(1);
  });

  it('serialize_toon_pdf: Serialize PDF extraction to TOON', () => {
    const options = new JsExtractionConfig();
    const result = serialize_to_toon("test_documents/pdf/sample.pdf", options);
    expect(result.content.length).toBeGreaterThanOrEqual(10);
  });
});
