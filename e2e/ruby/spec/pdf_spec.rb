# This file is auto-generated by alef. DO NOT EDIT.
# frozen_string_literal: true

require 'kreuzberg'
require 'json'

RSpec.describe 'pdf' do
  it 'pdf_assembly_technical: Assembly language technical manual with large body of text.' do
    result = Kreuzberg.extract_file('')
    expect(result.mime_type).to eq('application/pdf')
    expect(result.content.length).to be >= 5000
    expect(['assembly', 'register', 'instruction'].any? { |v| result.content.to_s.include?(v) }).to be(true)
    expect(result.metadata.format_type).to eq('pdf')
  end

  it 'pdf_bayesian_data_analysis: Bayesian data analysis textbook PDF with large content volume.' do
    result = Kreuzberg.extract_file('')
    expect(result.mime_type).to eq('application/pdf')
    expect(result.content.length).to be >= 10000
    expect(['Bayesian', 'probability', 'distribution'].any? { |v| result.content.to_s.include?(v) }).to be(true)
    expect(result.metadata.format_type).to eq('pdf')
  end

  it 'pdf_code_and_formula: PDF containing code snippets and formulas should retain substantial content.' do
    result = Kreuzberg.extract_file('')
    expect(result.mime_type).to eq('application/pdf')
    expect(result.content.length).to be >= 100
  end

  it 'pdf_deep_learning: Deep learning textbook PDF to ensure long-form extraction quality.' do
    result = Kreuzberg.extract_file('')
    expect(result.mime_type).to eq('application/pdf')
    expect(result.content.length).to be >= 1000
    expect(['neural', 'network', 'deep learning'].any? { |v| result.content.to_s.include?(v) }).to be(true)
    expect(result.metadata.format_type).to eq('pdf')
  end

  it 'pdf_embedded_images: PDF with embedded images should extract text and tables when present.' do
    result = Kreuzberg.extract_file('')
    expect(result.mime_type).to eq('application/pdf')
    expect(result.content.length).to be >= 50
    expect(result.tables.length).to be >= 0
  end

  it 'pdf_google_doc: Google Docs exported PDF to verify conversion fidelity.' do
    result = Kreuzberg.extract_file('')
    expect(result.mime_type).to eq('application/pdf')
    expect(result.content.length).to be >= 50
    expect(result.metadata.format_type).to eq('pdf')
  end

  it 'pdf_large_ciml: Large machine learning textbook PDF to stress extraction length.' do
    result = Kreuzberg.extract_file('')
    expect(result.mime_type).to eq('application/pdf')
    expect(result.content.length).to be >= 10000
    expect(['machine learning', 'algorithm', 'training'].any? { |v| result.content.to_s.include?(v) }).to be(true)
    expect(result.metadata.format_type).to eq('pdf')
  end

  it 'pdf_non_english_german: German technical PDF to ensure non-ASCII content extraction.' do
    result = Kreuzberg.extract_file('')
    expect(result.mime_type).to eq('application/pdf')
    expect(result.content.length).to be >= 100
    expect(['Intel', 'paging'].any? { |v| result.content.to_s.include?(v) }).to be(true)
    expect(result.metadata.format_type).to eq('pdf')
  end

  it 'pdf_right_to_left: Right-to-left language PDF to verify RTL extraction.' do
    result = Kreuzberg.extract_file('')
    expect(result.mime_type).to eq('application/pdf')
    expect(result.content.length).to be >= 50
    expect(result.metadata.format_type).to eq('pdf')
  end

  it 'pdf_simple_text: Simple text-heavy PDF should extract content without OCR or tables.' do
    result = Kreuzberg.extract_file('')
    expect(result.mime_type).to eq('application/pdf')
    expect(result.content.length).to be >= 50
    expect(['May 5, 2023', 'To Whom it May Concern', 'Mallori'].any? { |v| result.content.to_s.include?(v) }).to be(true)
  end

  it 'pdf_tables_large: Large PDF with extensive tables to stress table extraction.' do
    result = Kreuzberg.extract_file('')
    expect(result.mime_type).to eq('application/pdf')
    expect(result.content.length).to be >= 500
  end

  it 'pdf_tables_medium: Medium-sized PDF with multiple tables.' do
    result = Kreuzberg.extract_file('')
    expect(result.mime_type).to eq('application/pdf')
    expect(result.content.length).to be >= 100
  end

  it 'pdf_technical_stat_learning: Technical statistical learning PDF requiring substantial extraction.' do
    result = Kreuzberg.extract_file('')
    expect(result.mime_type).to eq('application/pdf')
    expect(result.content.length).to be >= 10000
    expect(['statistical', 'regression', 'learning'].any? { |v| result.content.to_s.include?(v) }).to be(true)
    expect(result.metadata.format_type).to eq('pdf')
  end
end
