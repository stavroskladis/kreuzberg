# This file is auto-generated by alef — DO NOT EDIT.
# alef:hash:c6a2da5d2095aa3072a72bacc5021499dc38b5b3975bc6483d21081b87bfb2e7
# To regenerate: alef generate
# To verify freshness: alef verify --exit-code
# Issues & docs: https://github.com/kreuzberg-dev/alef
# E2e tests for category: format_specific

test_that("format_csv: CSV file extraction using extract_file_sync", {
  result <- extract_file_sync(path = "csv/data_table.csv")
  expect_true(nchar(result$content) >= 5)
})

test_that("format_docx_standalone: Standalone DOCX extraction using extract_bytes_sync", {
  result <- extract_bytes_sync(mime_type = "application/vnd.openxmlformats-officedocument.wordprocessingml.document")
  expect_true(nchar(result$content) >= 20)
})

test_that("format_email_eml: Email EML file extraction using extract_file_sync", {
  result <- extract_file_sync(path = "email/html_email_multipart.eml")
  expect_true(nchar(result$content) >= 10)
})

test_that("format_pdf_text: Standalone PDF text extraction using extract_bytes_sync", {
  result <- extract_bytes_sync(mime_type = "application/pdf")
  expect_true(nchar(result$content) >= 50)
  expect_true(any(sapply(c("Mallori", "May"), function(v) grepl(v, result$content, fixed = TRUE))))
})

test_that("format_pptx: PPTX presentation extraction using extract_bytes_sync", {
  result <- extract_bytes_sync(mime_type = "application/vnd.openxmlformats-officedocument.presentationml.presentation")
})

test_that("format_rtf: RTF file extraction using extract_file_sync", {
  result <- extract_file_sync(path = "rtf/extraction_test.rtf")
  expect_true(nchar(result$content) >= 5)
})

test_that("format_xlsx: XLSX spreadsheet extraction using extract_bytes_sync", {
  result <- extract_bytes_sync(mime_type = "application/vnd.openxmlformats-officedocument.spreadsheetml.sheet")
})
