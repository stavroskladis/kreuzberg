// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:f5919501d9f0da7859da749c5c3d01c50fb6d1d115f78e30c8f794515bdf3642
// To regenerate: alef generate
// To verify freshness: alef verify --exit-code
// Issues & docs: https://github.com/kreuzberg-dev/alef
package dev.kreuzberg.e2e;

import org.junit.jupiter.api.Test;
import static org.junit.jupiter.api.Assertions.*;
import dev.kreuzberg.Kreuzberg;
import com.fasterxml.jackson.databind.ObjectMapper;
import com.fasterxml.jackson.datatype.jdk8.Jdk8Module;
import dev.kreuzberg.ExtractionConfig;

/** E2e tests for category: async. */
class AsyncTest {

    private static final ObjectMapper MAPPER = new ObjectMapper().registerModule(new Jdk8Module());
    @Test
    void testAsyncExtractBytes() throws Exception {
        // Async extract_bytes call on PDF document
        var result = Kreuzberg.extractBytes(null, "application/pdf");
        assertEquals("application/pdf", result.mimeType().trim());
        assertTrue(result.content().length() >= 50, "expected length >= 50");
    }

    @Test
    void testAsyncExtractBytesEmptyMime() throws Exception {
        // extract_bytes empty MIME async
        assertThrows(Exception.class, () -> Kreuzberg.extractBytes("SGVsbG8=".getBytes(), "", config));
    }

    @Test
    void testAsyncExtractBytesInvalidMime() throws Exception {
        // extract_bytes unsupported MIME async
        assertThrows(Exception.class, () -> Kreuzberg.extractBytes("SGVsbG8=".getBytes(), "application/x-nonexistent", config));
    }

    @Test
    void testAsyncExtractFile() throws Exception {
        // Async extract_file call on PDF document
        var result = Kreuzberg.extractFile("pdf/fake_memo.pdf", "application/pdf");
        assertEquals("application/pdf", result.mimeType().trim());
        assertTrue(result.content().length() >= 50, "expected length >= 50");
    }

    @Test
    void testAsyncExtractFileDocx() throws Exception {
        // Async extract_file call on DOCX document
        var result = Kreuzberg.extractFile("docx/fake.docx", "application/vnd.openxmlformats-officedocument.wordprocessingml.document");
        assertTrue(result.content().length() >= 20, "expected length >= 20");
    }

    @Test
    void testAsyncExtractFileHtml() throws Exception {
        // Async extract_file call on HTML document
        var result = Kreuzberg.extractFile("html/simple_table.html", "text/html");
        assertTrue(result.content().length() >= 10, "expected length >= 10");
    }

    @Test
    void testAsyncExtractFileImage() throws Exception {
        // Async extract_file call on image document
        var result = Kreuzberg.extractFile("images/test_hello_world.png", "image/png");
    }

    @Test
    void testAsyncExtractFileWithConfig() throws Exception {
        // Async extract_file call with output format and chunking config
        var result = Kreuzberg.extractFile("pdf/fake_memo.pdf", "application/pdf", config);
        assertTrue(result.content().length() >= 10, "expected length >= 10");
    }

}
