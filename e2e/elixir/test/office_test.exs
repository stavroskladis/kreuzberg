# This file is auto-generated by alef. DO NOT EDIT.
# E2e tests for category: office
defmodule E2e.OfficeTest do
  use ExUnit.Case, async: true

  describe "office_bibtex_basic" do
    test "BibTeX bibliography extraction." do
      {:ok, result} = Kreuzberg.extract_file_async("")
      assert Enum.any?(["application/x-bibtex", "text/x-bibtex"], fn v -> String.contains?(to_string(result.mime_type), v) end)
      assert String.length(result.content) >= 10
    end
  end

  describe "office_commonmark_basic" do
    test "CommonMark (.commonmark) text extraction." do
      {:ok, result} = Kreuzberg.extract_file_async("")
      assert Enum.any?(["text/markdown", "text/plain", "text/x-commonmark"], fn v -> String.contains?(to_string(result.mime_type), v) end)
      assert String.length(result.content) >= 5
    end
  end

  describe "office_djot_basic" do
    test "Djot markup text extraction." do
      {:ok, result} = Kreuzberg.extract_file_async("")
      assert Enum.any?(["text/x-djot", "text/djot"], fn v -> String.contains?(to_string(result.mime_type), v) end)
      assert String.length(result.content) >= 10
    end
  end

  describe "office_docbook_basic" do
    test "DocBook XML document extraction." do
      {:ok, result} = Kreuzberg.extract_file_async("")
      assert Enum.any?(["application/docbook+xml", "text/docbook"], fn v -> String.contains?(to_string(result.mime_type), v) end)
      assert String.length(result.content) >= 10
    end
  end

  describe "office_docx_basic" do
    test "DOCX document extraction baseline." do
      {:ok, result} = Kreuzberg.extract_file_async("")
      assert String.trim(result.mime_type) == "application/vnd.openxmlformats-officedocument.wordprocessingml.document"
      assert String.length(result.content) >= 10
    end
  end

  describe "office_docx_equations" do
    test "DOCX file containing equations to validate math extraction." do
      {:ok, result} = Kreuzberg.extract_file_async("")
      assert String.trim(result.mime_type) == "application/vnd.openxmlformats-officedocument.wordprocessingml.document"
      assert String.length(result.content) >= 20
    end
  end

  describe "office_docx_fake" do
    test "Simple DOCX document to verify baseline extraction." do
      {:ok, result} = Kreuzberg.extract_file_async("")
      assert String.trim(result.mime_type) == "application/vnd.openxmlformats-officedocument.wordprocessingml.document"
      assert String.length(result.content) >= 20
    end
  end

  describe "office_docx_formatting" do
    test "DOCX document heavy on formatting for style preservation." do
      {:ok, result} = Kreuzberg.extract_file_async("")
      assert String.trim(result.mime_type) == "application/vnd.openxmlformats-officedocument.wordprocessingml.document"
      assert String.length(result.content) >= 20
    end
  end

  describe "office_docx_headers" do
    test "DOCX document with complex headers." do
      {:ok, result} = Kreuzberg.extract_file_async("")
      assert String.trim(result.mime_type) == "application/vnd.openxmlformats-officedocument.wordprocessingml.document"
      assert String.length(result.content) >= 20
    end
  end

  describe "office_docx_lists" do
    test "DOCX document emphasizing list formatting." do
      {:ok, result} = Kreuzberg.extract_file_async("")
      assert String.trim(result.mime_type) == "application/vnd.openxmlformats-officedocument.wordprocessingml.document"
      assert String.length(result.content) >= 20
    end
  end

  describe "office_docx_tables" do
    test "DOCX document containing tables for table-aware extraction." do
      {:ok, result} = Kreuzberg.extract_file_async("")
      assert String.trim(result.mime_type) == "application/vnd.openxmlformats-officedocument.wordprocessingml.document"
      assert String.length(result.content) >= 50
      assert String.contains?(to_string(result.content), "Simple uniform table")
      assert String.contains?(to_string(result.content), "Nested Table")
      assert String.contains?(to_string(result.content), "merged cells")
      assert String.contains?(to_string(result.content), "Header Col")
      assert length(result.tables) >= 1
    end
  end

  describe "office_epub_basic" do
    test "EPUB book extraction with text content." do
      {:ok, result} = Kreuzberg.extract_file_async("")
      assert String.trim(result.mime_type) == "application/epub+zip"
      assert String.length(result.content) >= 50
    end
  end

  describe "office_fb2_basic" do
    test "FictionBook (FB2) document extraction baseline." do
      {:ok, result} = Kreuzberg.extract_file_async("")
      assert String.trim(result.mime_type) == "application/x-fictionbook+xml"
      assert String.length(result.content) >= 10
    end
  end

  describe "office_fictionbook_basic" do
    test "FictionBook (.fb2) text extraction." do
      {:ok, result} = Kreuzberg.extract_file_async("")
      assert Enum.any?(["application/x-fictionbook+xml", "application/x-fictionbook"], fn v -> String.contains?(to_string(result.mime_type), v) end)
      assert String.length(result.content) >= 10
    end
  end

  describe "office_jats_basic" do
    test "JATS scientific article extraction." do
      {:ok, result} = Kreuzberg.extract_file_async("")
      assert Enum.any?(["application/x-jats+xml", "text/jats"], fn v -> String.contains?(to_string(result.mime_type), v) end)
      assert String.length(result.content) >= 10
    end
  end

  describe "office_jupyter_basic" do
    test "Jupyter notebook extraction." do
      {:ok, result} = Kreuzberg.extract_file_async("")
      assert String.trim(result.mime_type) == "application/x-ipynb+json"
      assert String.length(result.content) >= 10
    end
  end

  describe "office_keynote_basic" do
    test "Keynote document extraction baseline." do
      {:ok, result} = Kreuzberg.extract_file_async("")
      assert String.trim(result.mime_type) == "application/x-iwork-keynote-sffkey"
      assert String.length(result.content) >= 5
    end
  end

  describe "office_latex_basic" do
    test "LaTeX document text extraction." do
      {:ok, result} = Kreuzberg.extract_file_async("")
      assert Enum.any?(["application/x-latex", "text/x-latex"], fn v -> String.contains?(to_string(result.mime_type), v) end)
      assert String.length(result.content) >= 20
    end
  end

  describe "office_markdown_basic" do
    test "Markdown document extraction baseline." do
      {:ok, result} = Kreuzberg.extract_file_async("")
      assert String.trim(result.mime_type) == "text/markdown"
      assert String.length(result.content) >= 10
    end
  end

  describe "office_numbers_basic" do
    test "Numbers document extraction baseline." do
      {:ok, result} = Kreuzberg.extract_file_async("")
      assert String.trim(result.mime_type) == "application/x-iwork-numbers-sffnumbers"
      assert String.length(result.content) >= 10
    end
  end

  describe "office_ods_basic" do
    test "Basic ODS spreadsheet extraction." do
      {:ok, result} = Kreuzberg.extract_file_async("")
      assert String.trim(result.mime_type) == "application/vnd.oasis.opendocument.spreadsheet"
      assert String.length(result.content) >= 10
    end
  end

  describe "office_odt_bold" do
    test "ODT document with bold formatting." do
      {:ok, result} = Kreuzberg.extract_file_async("")
      assert String.trim(result.mime_type) == "application/vnd.oasis.opendocument.text"
      assert String.length(result.content) >= 10
    end
  end

  describe "office_odt_list" do
    test "ODT document containing unordered lists with nesting." do
      {:ok, result} = Kreuzberg.extract_file_async("")
      assert String.trim(result.mime_type) == "application/vnd.oasis.opendocument.text"
      assert String.length(result.content) >= 30
      assert Enum.any?(["list item", "New level", "Pushed us"], fn v -> String.contains?(to_string(result.content), v) end)
    end
  end

  describe "office_odt_simple" do
    test "Basic ODT document with paragraphs and headings." do
      {:ok, result} = Kreuzberg.extract_file_async("")
      assert String.trim(result.mime_type) == "application/vnd.oasis.opendocument.text"
      assert String.length(result.content) >= 50
      assert Enum.any?(["favorite things", "Parrots", "Analysis"], fn v -> String.contains?(to_string(result.content), v) end)
    end
  end

  describe "office_odt_table" do
    test "ODT document with a table structure." do
      {:ok, result} = Kreuzberg.extract_file_async("")
      assert String.trim(result.mime_type) == "application/vnd.oasis.opendocument.text"
      assert String.length(result.content) >= 10
      assert length(result.tables) >= 1
    end
  end

  describe "office_opml_basic" do
    test "OPML outline document extraction." do
      {:ok, result} = Kreuzberg.extract_file_async("")
      assert Enum.any?(["application/xml+opml", "text/x-opml", "application/x-opml+xml"], fn v -> String.contains?(to_string(result.mime_type), v) end)
      assert String.length(result.content) >= 10
    end
  end

  describe "office_org_basic" do
    test "Org-mode document text extraction." do
      {:ok, result} = Kreuzberg.extract_file_async("")
      assert Enum.any?(["text/x-org", "text/org"], fn v -> String.contains?(to_string(result.mime_type), v) end)
      assert String.length(result.content) >= 20
    end
  end

  describe "office_pages_basic" do
    test "Pages document extraction baseline." do
      {:ok, result} = Kreuzberg.extract_file_async("")
      assert String.trim(result.mime_type) == "application/x-iwork-pages-sffpages"
      assert String.length(result.content) >= 5
    end
  end

  describe "office_ppsx_slideshow" do
    test "PPSX (PowerPoint Show) files should extract slides content identical to PPTX. GitHub Issue #321 Bug 2." do
      {:ok, result} = Kreuzberg.extract_file_async("")
      assert String.trim(result.mime_type) == "application/vnd.openxmlformats-officedocument.presentationml.slideshow"
      assert String.length(result.content) >= 10
    end
  end

  describe "office_pptm_basic" do
    test "PowerPoint macro-enabled presentation (.pptm) extraction." do
      {:ok, result} = Kreuzberg.extract_file_async("")
      assert Enum.any?(["application/vnd.ms-powerpoint.presentation.macroEnabled.12", "application/vnd.openxmlformats-officedocument.presentationml.presentation"], fn v -> String.contains?(to_string(result.mime_type), v) end)
      assert result.content != ""
    end
  end

  describe "office_pptx_basic" do
    test "PPTX deck should extract slides content." do
      {:ok, result} = Kreuzberg.extract_file_async("")
      assert String.trim(result.mime_type) == "application/vnd.openxmlformats-officedocument.presentationml.presentation"
      assert String.length(result.content) >= 50
    end
  end

  describe "office_pptx_images" do
    test "PPTX presentation containing images to ensure metadata extraction." do
      {:ok, result} = Kreuzberg.extract_file_async("")
      assert String.trim(result.mime_type) == "application/vnd.openxmlformats-officedocument.presentationml.presentation"
      assert String.length(result.content) >= 15
    end
  end

  describe "office_pptx_pitch_deck" do
    test "Pitch deck PPTX used to validate large slide extraction." do
      {:ok, result} = Kreuzberg.extract_file_async("")
      assert String.trim(result.mime_type) == "application/vnd.openxmlformats-officedocument.presentationml.presentation"
      assert String.length(result.content) >= 100
    end
  end

  describe "office_rst_basic" do
    test "reStructuredText document extraction." do
      {:ok, result} = Kreuzberg.extract_file_async("")
      assert Enum.any?(["text/x-rst", "text/prs.fallenstein.rst"], fn v -> String.contains?(to_string(result.mime_type), v) end)
      assert String.length(result.content) >= 20
    end
  end

  describe "office_rtf_basic" do
    test "RTF document text extraction." do
      {:ok, result} = Kreuzberg.extract_file_async("")
      assert Enum.any?(["application/rtf", "text/rtf"], fn v -> String.contains?(to_string(result.mime_type), v) end)
      assert String.length(result.content) >= 10
    end
  end

  describe "office_typst_basic" do
    test "Typst document text extraction." do
      {:ok, result} = Kreuzberg.extract_file_async("")
      assert Enum.any?(["application/x-typst", "text/x-typst"], fn v -> String.contains?(to_string(result.mime_type), v) end)
      assert String.length(result.content) >= 10
    end
  end

  describe "office_xls_legacy" do
    test "Legacy XLS spreadsheet to ensure backward compatibility." do
      {:ok, result} = Kreuzberg.extract_file_async("")
      assert String.trim(result.mime_type) == "application/vnd.ms-excel"
      assert String.length(result.content) >= 10
    end
  end

  describe "office_xlsb_basic" do
    test "Excel binary workbook (.xlsb) extraction." do
      {:ok, result} = Kreuzberg.extract_file_async("")
      assert Enum.any?(["application/vnd.ms-excel.sheet.binary.macroEnabled.12", "application/vnd.openxmlformats-officedocument.spreadsheetml.sheet"], fn v -> String.contains?(to_string(result.mime_type), v) end)
      assert result.content != ""
    end
  end

  describe "office_xlsm_basic" do
    test "Excel macro-enabled workbook (.xlsm) extraction." do
      {:ok, result} = Kreuzberg.extract_file_async("")
      assert Enum.any?(["application/vnd.ms-excel.sheet.macroEnabled.12", "application/vnd.openxmlformats-officedocument.spreadsheetml.sheet"], fn v -> String.contains?(to_string(result.mime_type), v) end)
      assert result.content != ""
    end
  end

  describe "office_xlsx_basic" do
    test "XLSX spreadsheet should produce metadata and table content." do
      {:ok, result} = Kreuzberg.extract_file_async("")
      assert String.trim(result.mime_type) == "application/vnd.openxmlformats-officedocument.spreadsheetml.sheet"
      assert String.length(result.content) >= 100
      assert String.contains?(to_string(result.content), "Team")
      assert String.contains?(to_string(result.content), "Location")
      assert String.contains?(to_string(result.content), "Stanley Cups")
      assert length(result.tables) >= 1
      assert result.metadata.sheet_count >= 2
      assert String.contains?(to_string(result.metadata.sheet_names), "Stanley Cups")
    end
  end

  describe "office_xlsx_multi_sheet" do
    test "XLSX workbook with multiple sheets." do
      {:ok, result} = Kreuzberg.extract_file_async("")
      assert String.trim(result.mime_type) == "application/vnd.openxmlformats-officedocument.spreadsheetml.sheet"
      assert String.length(result.content) >= 20
      assert result.metadata.sheet_count >= 2
    end
  end

  describe "office_xlsx_office_example" do
    test "Simple XLSX spreadsheet shipped alongside office integration tests." do
      {:ok, result} = Kreuzberg.extract_file_async("")
      assert String.trim(result.mime_type) == "application/vnd.openxmlformats-officedocument.spreadsheetml.sheet"
      assert String.length(result.content) >= 10
    end
  end
end
