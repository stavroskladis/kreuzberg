# This file is auto-generated by alef. DO NOT EDIT.
# E2e tests for category: mime_utilities
defmodule E2e.MimeUtilitiesTest do
  use ExUnit.Case, async: true

  describe "mime_detect_bytes" do
    test "Detect MIME type from file bytes" do
      {:ok, result} = Kreuzberg.extract_file_async("")
      assert String.contains?(to_string(result.result), "pdf")
    end
  end

  describe "mime_detect_path" do
    test "Detect MIME type from file path" do
      {:ok, result} = Kreuzberg.extract_file_async("")
      assert String.contains?(to_string(result.result), "text")
    end
  end

  describe "mime_get_extensions" do
    test "Get file extensions for a MIME type" do
      {:ok, result} = Kreuzberg.extract_file_async("")
      assert String.contains?(to_string(result.result), "pdf")
    end
  end
end
