// This file is auto-generated by alef. DO NOT EDIT.
//! E2e tests for category: office

use kreuzberg::extract_file;

#[tokio::test]
async fn test_office_bibtex_basic() {
    // BibTeX bibliography extraction.
    let path = String::new();
    let mime_type = None;
    let config = None;
    let result = extract_file(&path, &mime_type, config).await.expect("should succeed");
    assert!(
        result.mime_type.contains(r#"application/x-bibtex"#) || result.mime_type.contains(r#"text/x-bibtex"#),
        "expected to contain at least one of the specified values"
    );
    assert!(
        result.content.len() >= 10,
        "expected length >= 10, got {}",
        result.content.len()
    );
}

#[tokio::test]
async fn test_office_commonmark_basic() {
    // CommonMark (.commonmark) text extraction.
    let path = String::new();
    let mime_type = None;
    let config = None;
    let result = extract_file(&path, &mime_type, config).await.expect("should succeed");
    assert!(
        result.mime_type.contains(r#"text/markdown"#)
            || result.mime_type.contains(r#"text/plain"#)
            || result.mime_type.contains(r#"text/x-commonmark"#),
        "expected to contain at least one of the specified values"
    );
    assert!(
        result.content.len() >= 5,
        "expected length >= 5, got {}",
        result.content.len()
    );
}

#[tokio::test]
async fn test_office_djot_basic() {
    // Djot markup text extraction.
    let path = String::new();
    let mime_type = None;
    let config = None;
    let result = extract_file(&path, &mime_type, config).await.expect("should succeed");
    assert!(
        result.mime_type.contains(r#"text/x-djot"#) || result.mime_type.contains(r#"text/djot"#),
        "expected to contain at least one of the specified values"
    );
    assert!(
        result.content.len() >= 10,
        "expected length >= 10, got {}",
        result.content.len()
    );
}

#[tokio::test]
async fn test_office_docbook_basic() {
    // DocBook XML document extraction.
    let path = String::new();
    let mime_type = None;
    let config = None;
    let result = extract_file(&path, &mime_type, config).await.expect("should succeed");
    assert!(
        result.mime_type.contains(r#"application/docbook+xml"#) || result.mime_type.contains(r#"text/docbook"#),
        "expected to contain at least one of the specified values"
    );
    assert!(
        result.content.len() >= 10,
        "expected length >= 10, got {}",
        result.content.len()
    );
}

#[tokio::test]
async fn test_office_docx_basic() {
    // DOCX document extraction baseline.
    let path = String::new();
    let mime_type = None;
    let config = None;
    let result = extract_file(&path, &mime_type, config).await.expect("should succeed");
    assert_eq!(
        result.mime_type.trim(),
        r#"application/vnd.openxmlformats-officedocument.wordprocessingml.document"#,
        "equals assertion failed"
    );
    assert!(
        result.content.len() >= 10,
        "expected length >= 10, got {}",
        result.content.len()
    );
}

#[tokio::test]
async fn test_office_docx_equations() {
    // DOCX file containing equations to validate math extraction.
    let path = String::new();
    let mime_type = None;
    let config = None;
    let result = extract_file(&path, &mime_type, config).await.expect("should succeed");
    assert_eq!(
        result.mime_type.trim(),
        r#"application/vnd.openxmlformats-officedocument.wordprocessingml.document"#,
        "equals assertion failed"
    );
    assert!(
        result.content.len() >= 20,
        "expected length >= 20, got {}",
        result.content.len()
    );
}

#[tokio::test]
async fn test_office_docx_fake() {
    // Simple DOCX document to verify baseline extraction.
    let path = String::new();
    let mime_type = None;
    let config = None;
    let result = extract_file(&path, &mime_type, config).await.expect("should succeed");
    assert_eq!(
        result.mime_type.trim(),
        r#"application/vnd.openxmlformats-officedocument.wordprocessingml.document"#,
        "equals assertion failed"
    );
    assert!(
        result.content.len() >= 20,
        "expected length >= 20, got {}",
        result.content.len()
    );
}

#[tokio::test]
async fn test_office_docx_formatting() {
    // DOCX document heavy on formatting for style preservation.
    let path = String::new();
    let mime_type = None;
    let config = None;
    let result = extract_file(&path, &mime_type, config).await.expect("should succeed");
    assert_eq!(
        result.mime_type.trim(),
        r#"application/vnd.openxmlformats-officedocument.wordprocessingml.document"#,
        "equals assertion failed"
    );
    assert!(
        result.content.len() >= 20,
        "expected length >= 20, got {}",
        result.content.len()
    );
}

#[tokio::test]
async fn test_office_docx_headers() {
    // DOCX document with complex headers.
    let path = String::new();
    let mime_type = None;
    let config = None;
    let result = extract_file(&path, &mime_type, config).await.expect("should succeed");
    assert_eq!(
        result.mime_type.trim(),
        r#"application/vnd.openxmlformats-officedocument.wordprocessingml.document"#,
        "equals assertion failed"
    );
    assert!(
        result.content.len() >= 20,
        "expected length >= 20, got {}",
        result.content.len()
    );
}

#[tokio::test]
async fn test_office_docx_lists() {
    // DOCX document emphasizing list formatting.
    let path = String::new();
    let mime_type = None;
    let config = None;
    let result = extract_file(&path, &mime_type, config).await.expect("should succeed");
    assert_eq!(
        result.mime_type.trim(),
        r#"application/vnd.openxmlformats-officedocument.wordprocessingml.document"#,
        "equals assertion failed"
    );
    assert!(
        result.content.len() >= 20,
        "expected length >= 20, got {}",
        result.content.len()
    );
}

#[tokio::test]
async fn test_office_docx_tables() {
    // DOCX document containing tables for table-aware extraction.
    let path = String::new();
    let mime_type = None;
    let config = None;
    let result = extract_file(&path, &mime_type, config).await.expect("should succeed");
    let content = result.content.as_deref().unwrap_or("");
    assert_eq!(
        result.mime_type.trim(),
        r#"application/vnd.openxmlformats-officedocument.wordprocessingml.document"#,
        "equals assertion failed"
    );
    assert!(content.len() >= 50, "expected length >= 50, got {}", content.len());
    assert!(
        format!("{:?}", content).contains(r#"Simple uniform table"#),
        "expected to contain: {}",
        r#"Simple uniform table"#
    );
    assert!(
        format!("{:?}", content).contains(r#"Nested Table"#),
        "expected to contain: {}",
        r#"Nested Table"#
    );
    assert!(
        format!("{:?}", content).contains(r#"merged cells"#),
        "expected to contain: {}",
        r#"merged cells"#
    );
    assert!(
        format!("{:?}", content).contains(r#"Header Col"#),
        "expected to contain: {}",
        r#"Header Col"#
    );
    assert!(
        result.tables.len() >= 1,
        "expected at least 1 elements, got {}",
        result.tables.len()
    );
}

#[tokio::test]
async fn test_office_epub_basic() {
    // EPUB book extraction with text content.
    let path = String::new();
    let mime_type = None;
    let config = None;
    let result = extract_file(&path, &mime_type, config).await.expect("should succeed");
    assert_eq!(
        result.mime_type.trim(),
        r#"application/epub+zip"#,
        "equals assertion failed"
    );
    assert!(
        result.content.len() >= 50,
        "expected length >= 50, got {}",
        result.content.len()
    );
}

#[tokio::test]
async fn test_office_fb2_basic() {
    // FictionBook (FB2) document extraction baseline.
    let path = String::new();
    let mime_type = None;
    let config = None;
    let result = extract_file(&path, &mime_type, config).await.expect("should succeed");
    assert_eq!(
        result.mime_type.trim(),
        r#"application/x-fictionbook+xml"#,
        "equals assertion failed"
    );
    assert!(
        result.content.len() >= 10,
        "expected length >= 10, got {}",
        result.content.len()
    );
}

#[tokio::test]
async fn test_office_fictionbook_basic() {
    // FictionBook (.fb2) text extraction.
    let path = String::new();
    let mime_type = None;
    let config = None;
    let result = extract_file(&path, &mime_type, config).await.expect("should succeed");
    assert!(
        result.mime_type.contains(r#"application/x-fictionbook+xml"#)
            || result.mime_type.contains(r#"application/x-fictionbook"#),
        "expected to contain at least one of the specified values"
    );
    assert!(
        result.content.len() >= 10,
        "expected length >= 10, got {}",
        result.content.len()
    );
}

#[tokio::test]
async fn test_office_jats_basic() {
    // JATS scientific article extraction.
    let path = String::new();
    let mime_type = None;
    let config = None;
    let result = extract_file(&path, &mime_type, config).await.expect("should succeed");
    assert!(
        result.mime_type.contains(r#"application/x-jats+xml"#) || result.mime_type.contains(r#"text/jats"#),
        "expected to contain at least one of the specified values"
    );
    assert!(
        result.content.len() >= 10,
        "expected length >= 10, got {}",
        result.content.len()
    );
}

#[tokio::test]
async fn test_office_jupyter_basic() {
    // Jupyter notebook extraction.
    let path = String::new();
    let mime_type = None;
    let config = None;
    let result = extract_file(&path, &mime_type, config).await.expect("should succeed");
    assert_eq!(
        result.mime_type.trim(),
        r#"application/x-ipynb+json"#,
        "equals assertion failed"
    );
    assert!(
        result.content.len() >= 10,
        "expected length >= 10, got {}",
        result.content.len()
    );
}

#[tokio::test]
async fn test_office_keynote_basic() {
    // Keynote document extraction baseline.
    let path = String::new();
    let mime_type = None;
    let config = None;
    let result = extract_file(&path, &mime_type, config).await.expect("should succeed");
    assert_eq!(
        result.mime_type.trim(),
        r#"application/x-iwork-keynote-sffkey"#,
        "equals assertion failed"
    );
    assert!(
        result.content.len() >= 5,
        "expected length >= 5, got {}",
        result.content.len()
    );
}

#[tokio::test]
async fn test_office_latex_basic() {
    // LaTeX document text extraction.
    let path = String::new();
    let mime_type = None;
    let config = None;
    let result = extract_file(&path, &mime_type, config).await.expect("should succeed");
    assert!(
        result.mime_type.contains(r#"application/x-latex"#) || result.mime_type.contains(r#"text/x-latex"#),
        "expected to contain at least one of the specified values"
    );
    assert!(
        result.content.len() >= 20,
        "expected length >= 20, got {}",
        result.content.len()
    );
}

#[tokio::test]
async fn test_office_markdown_basic() {
    // Markdown document extraction baseline.
    let path = String::new();
    let mime_type = None;
    let config = None;
    let result = extract_file(&path, &mime_type, config).await.expect("should succeed");
    assert_eq!(result.mime_type.trim(), r#"text/markdown"#, "equals assertion failed");
    assert!(
        result.content.len() >= 10,
        "expected length >= 10, got {}",
        result.content.len()
    );
}

#[tokio::test]
async fn test_office_numbers_basic() {
    // Numbers document extraction baseline.
    let path = String::new();
    let mime_type = None;
    let config = None;
    let result = extract_file(&path, &mime_type, config).await.expect("should succeed");
    assert_eq!(
        result.mime_type.trim(),
        r#"application/x-iwork-numbers-sffnumbers"#,
        "equals assertion failed"
    );
    assert!(
        result.content.len() >= 10,
        "expected length >= 10, got {}",
        result.content.len()
    );
}

#[tokio::test]
async fn test_office_ods_basic() {
    // Basic ODS spreadsheet extraction.
    let path = String::new();
    let mime_type = None;
    let config = None;
    let result = extract_file(&path, &mime_type, config).await.expect("should succeed");
    assert_eq!(
        result.mime_type.trim(),
        r#"application/vnd.oasis.opendocument.spreadsheet"#,
        "equals assertion failed"
    );
    assert!(
        result.content.len() >= 10,
        "expected length >= 10, got {}",
        result.content.len()
    );
}

#[tokio::test]
async fn test_office_odt_bold() {
    // ODT document with bold formatting.
    let path = String::new();
    let mime_type = None;
    let config = None;
    let result = extract_file(&path, &mime_type, config).await.expect("should succeed");
    assert_eq!(
        result.mime_type.trim(),
        r#"application/vnd.oasis.opendocument.text"#,
        "equals assertion failed"
    );
    assert!(
        result.content.len() >= 10,
        "expected length >= 10, got {}",
        result.content.len()
    );
}

#[tokio::test]
async fn test_office_odt_list() {
    // ODT document containing unordered lists with nesting.
    let path = String::new();
    let mime_type = None;
    let config = None;
    let result = extract_file(&path, &mime_type, config).await.expect("should succeed");
    let content = result.content.as_deref().unwrap_or("");
    assert_eq!(
        result.mime_type.trim(),
        r#"application/vnd.oasis.opendocument.text"#,
        "equals assertion failed"
    );
    assert!(content.len() >= 30, "expected length >= 30, got {}", content.len());
    assert!(
        content.contains(r#"list item"#) || content.contains(r#"New level"#) || content.contains(r#"Pushed us"#),
        "expected to contain at least one of the specified values"
    );
}

#[tokio::test]
async fn test_office_odt_simple() {
    // Basic ODT document with paragraphs and headings.
    let path = String::new();
    let mime_type = None;
    let config = None;
    let result = extract_file(&path, &mime_type, config).await.expect("should succeed");
    let content = result.content.as_deref().unwrap_or("");
    assert_eq!(
        result.mime_type.trim(),
        r#"application/vnd.oasis.opendocument.text"#,
        "equals assertion failed"
    );
    assert!(content.len() >= 50, "expected length >= 50, got {}", content.len());
    assert!(
        content.contains(r#"favorite things"#) || content.contains(r#"Parrots"#) || content.contains(r#"Analysis"#),
        "expected to contain at least one of the specified values"
    );
}

#[tokio::test]
async fn test_office_odt_table() {
    // ODT document with a table structure.
    let path = String::new();
    let mime_type = None;
    let config = None;
    let result = extract_file(&path, &mime_type, config).await.expect("should succeed");
    assert_eq!(
        result.mime_type.trim(),
        r#"application/vnd.oasis.opendocument.text"#,
        "equals assertion failed"
    );
    assert!(
        result.content.len() >= 10,
        "expected length >= 10, got {}",
        result.content.len()
    );
    assert!(
        result.tables.len() >= 1,
        "expected at least 1 elements, got {}",
        result.tables.len()
    );
}

#[tokio::test]
async fn test_office_opml_basic() {
    // OPML outline document extraction.
    let path = String::new();
    let mime_type = None;
    let config = None;
    let result = extract_file(&path, &mime_type, config).await.expect("should succeed");
    assert!(
        result.mime_type.contains(r#"application/xml+opml"#)
            || result.mime_type.contains(r#"text/x-opml"#)
            || result.mime_type.contains(r#"application/x-opml+xml"#),
        "expected to contain at least one of the specified values"
    );
    assert!(
        result.content.len() >= 10,
        "expected length >= 10, got {}",
        result.content.len()
    );
}

#[tokio::test]
async fn test_office_org_basic() {
    // Org-mode document text extraction.
    let path = String::new();
    let mime_type = None;
    let config = None;
    let result = extract_file(&path, &mime_type, config).await.expect("should succeed");
    assert!(
        result.mime_type.contains(r#"text/x-org"#) || result.mime_type.contains(r#"text/org"#),
        "expected to contain at least one of the specified values"
    );
    assert!(
        result.content.len() >= 20,
        "expected length >= 20, got {}",
        result.content.len()
    );
}

#[tokio::test]
async fn test_office_pages_basic() {
    // Pages document extraction baseline.
    let path = String::new();
    let mime_type = None;
    let config = None;
    let result = extract_file(&path, &mime_type, config).await.expect("should succeed");
    assert_eq!(
        result.mime_type.trim(),
        r#"application/x-iwork-pages-sffpages"#,
        "equals assertion failed"
    );
    assert!(
        result.content.len() >= 5,
        "expected length >= 5, got {}",
        result.content.len()
    );
}

#[tokio::test]
async fn test_office_ppsx_slideshow() {
    // PPSX (PowerPoint Show) files should extract slides content identical to PPTX. GitHub Issue #321 Bug 2.
    let path = String::new();
    let mime_type = None;
    let config = None;
    let result = extract_file(&path, &mime_type, config).await.expect("should succeed");
    assert_eq!(
        result.mime_type.trim(),
        r#"application/vnd.openxmlformats-officedocument.presentationml.slideshow"#,
        "equals assertion failed"
    );
    assert!(
        result.content.len() >= 10,
        "expected length >= 10, got {}",
        result.content.len()
    );
}

#[tokio::test]
async fn test_office_pptm_basic() {
    // PowerPoint macro-enabled presentation (.pptm) extraction.
    let path = String::new();
    let mime_type = None;
    let config = None;
    let result = extract_file(&path, &mime_type, config).await.expect("should succeed");
    assert!(
        result
            .mime_type
            .contains(r#"application/vnd.ms-powerpoint.presentation.macroEnabled.12"#)
            || result
                .mime_type
                .contains(r#"application/vnd.openxmlformats-officedocument.presentationml.presentation"#),
        "expected to contain at least one of the specified values"
    );
    assert!(result.content.is_some(), "expected content to be present");
}

#[tokio::test]
async fn test_office_pptx_basic() {
    // PPTX deck should extract slides content.
    let path = String::new();
    let mime_type = None;
    let config = None;
    let result = extract_file(&path, &mime_type, config).await.expect("should succeed");
    assert_eq!(
        result.mime_type.trim(),
        r#"application/vnd.openxmlformats-officedocument.presentationml.presentation"#,
        "equals assertion failed"
    );
    assert!(
        result.content.len() >= 50,
        "expected length >= 50, got {}",
        result.content.len()
    );
}

#[tokio::test]
async fn test_office_pptx_images() {
    // PPTX presentation containing images to ensure metadata extraction.
    let path = String::new();
    let mime_type = None;
    let config = None;
    let result = extract_file(&path, &mime_type, config).await.expect("should succeed");
    assert_eq!(
        result.mime_type.trim(),
        r#"application/vnd.openxmlformats-officedocument.presentationml.presentation"#,
        "equals assertion failed"
    );
    assert!(
        result.content.len() >= 15,
        "expected length >= 15, got {}",
        result.content.len()
    );
}

#[tokio::test]
async fn test_office_pptx_pitch_deck() {
    // Pitch deck PPTX used to validate large slide extraction.
    let path = String::new();
    let mime_type = None;
    let config = None;
    let result = extract_file(&path, &mime_type, config).await.expect("should succeed");
    assert_eq!(
        result.mime_type.trim(),
        r#"application/vnd.openxmlformats-officedocument.presentationml.presentation"#,
        "equals assertion failed"
    );
    assert!(
        result.content.len() >= 100,
        "expected length >= 100, got {}",
        result.content.len()
    );
}

#[tokio::test]
async fn test_office_rst_basic() {
    // reStructuredText document extraction.
    let path = String::new();
    let mime_type = None;
    let config = None;
    let result = extract_file(&path, &mime_type, config).await.expect("should succeed");
    assert!(
        result.mime_type.contains(r#"text/x-rst"#) || result.mime_type.contains(r#"text/prs.fallenstein.rst"#),
        "expected to contain at least one of the specified values"
    );
    assert!(
        result.content.len() >= 20,
        "expected length >= 20, got {}",
        result.content.len()
    );
}

#[tokio::test]
async fn test_office_rtf_basic() {
    // RTF document text extraction.
    let path = String::new();
    let mime_type = None;
    let config = None;
    let result = extract_file(&path, &mime_type, config).await.expect("should succeed");
    assert!(
        result.mime_type.contains(r#"application/rtf"#) || result.mime_type.contains(r#"text/rtf"#),
        "expected to contain at least one of the specified values"
    );
    assert!(
        result.content.len() >= 10,
        "expected length >= 10, got {}",
        result.content.len()
    );
}

#[tokio::test]
async fn test_office_typst_basic() {
    // Typst document text extraction.
    let path = String::new();
    let mime_type = None;
    let config = None;
    let result = extract_file(&path, &mime_type, config).await.expect("should succeed");
    assert!(
        result.mime_type.contains(r#"application/x-typst"#) || result.mime_type.contains(r#"text/x-typst"#),
        "expected to contain at least one of the specified values"
    );
    assert!(
        result.content.len() >= 10,
        "expected length >= 10, got {}",
        result.content.len()
    );
}

#[tokio::test]
async fn test_office_xls_legacy() {
    // Legacy XLS spreadsheet to ensure backward compatibility.
    let path = String::new();
    let mime_type = None;
    let config = None;
    let result = extract_file(&path, &mime_type, config).await.expect("should succeed");
    assert_eq!(
        result.mime_type.trim(),
        r#"application/vnd.ms-excel"#,
        "equals assertion failed"
    );
    assert!(
        result.content.len() >= 10,
        "expected length >= 10, got {}",
        result.content.len()
    );
}

#[tokio::test]
async fn test_office_xlsb_basic() {
    // Excel binary workbook (.xlsb) extraction.
    let path = String::new();
    let mime_type = None;
    let config = None;
    let result = extract_file(&path, &mime_type, config).await.expect("should succeed");
    assert!(
        result
            .mime_type
            .contains(r#"application/vnd.ms-excel.sheet.binary.macroEnabled.12"#)
            || result
                .mime_type
                .contains(r#"application/vnd.openxmlformats-officedocument.spreadsheetml.sheet"#),
        "expected to contain at least one of the specified values"
    );
    assert!(result.content.is_some(), "expected content to be present");
}

#[tokio::test]
async fn test_office_xlsm_basic() {
    // Excel macro-enabled workbook (.xlsm) extraction.
    let path = String::new();
    let mime_type = None;
    let config = None;
    let result = extract_file(&path, &mime_type, config).await.expect("should succeed");
    assert!(
        result
            .mime_type
            .contains(r#"application/vnd.ms-excel.sheet.macroEnabled.12"#)
            || result
                .mime_type
                .contains(r#"application/vnd.openxmlformats-officedocument.spreadsheetml.sheet"#),
        "expected to contain at least one of the specified values"
    );
    assert!(result.content.is_some(), "expected content to be present");
}

#[tokio::test]
async fn test_office_xlsx_basic() {
    // XLSX spreadsheet should produce metadata and table content.
    let path = String::new();
    let mime_type = None;
    let config = None;
    let result = extract_file(&path, &mime_type, config).await.expect("should succeed");
    let content = result.content.as_deref().unwrap_or("");
    assert_eq!(
        result.mime_type.trim(),
        r#"application/vnd.openxmlformats-officedocument.spreadsheetml.sheet"#,
        "equals assertion failed"
    );
    assert!(content.len() >= 100, "expected length >= 100, got {}", content.len());
    assert!(
        format!("{:?}", content).contains(r#"Team"#),
        "expected to contain: {}",
        r#"Team"#
    );
    assert!(
        format!("{:?}", content).contains(r#"Location"#),
        "expected to contain: {}",
        r#"Location"#
    );
    assert!(
        format!("{:?}", content).contains(r#"Stanley Cups"#),
        "expected to contain: {}",
        r#"Stanley Cups"#
    );
    assert!(
        result.tables.len() >= 1,
        "expected at least 1 elements, got {}",
        result.tables.len()
    );
    assert!(result.metadata.sheet_count >= 2, "expected >= 2");
    assert!(
        format!("{:?}", result.metadata.sheet_names).contains(r#"Stanley Cups"#),
        "expected to contain: {}",
        r#"Stanley Cups"#
    );
}

#[tokio::test]
async fn test_office_xlsx_multi_sheet() {
    // XLSX workbook with multiple sheets.
    let path = String::new();
    let mime_type = None;
    let config = None;
    let result = extract_file(&path, &mime_type, config).await.expect("should succeed");
    assert_eq!(
        result.mime_type.trim(),
        r#"application/vnd.openxmlformats-officedocument.spreadsheetml.sheet"#,
        "equals assertion failed"
    );
    assert!(
        result.content.len() >= 20,
        "expected length >= 20, got {}",
        result.content.len()
    );
    assert!(result.metadata.sheet_count >= 2, "expected >= 2");
}

#[tokio::test]
async fn test_office_xlsx_office_example() {
    // Simple XLSX spreadsheet shipped alongside office integration tests.
    let path = String::new();
    let mime_type = None;
    let config = None;
    let result = extract_file(&path, &mime_type, config).await.expect("should succeed");
    assert_eq!(
        result.mime_type.trim(),
        r#"application/vnd.openxmlformats-officedocument.spreadsheetml.sheet"#,
        "equals assertion failed"
    );
    assert!(
        result.content.len() >= 10,
        "expected length >= 10, got {}",
        result.content.len()
    );
}
