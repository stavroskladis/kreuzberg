// This file is auto-generated by alef. DO NOT EDIT.
//! E2e tests for category: pdf

use kreuzberg::extract_file;

#[tokio::test]
async fn test_pdf_assembly_technical() {
    // Assembly language technical manual with large body of text.
    let path = String::new();
    let mime_type = None;
    let config = None;
    let result = extract_file(&path, &mime_type, config).await.expect("should succeed");
    let content = result.content.as_deref().unwrap_or("");
    assert_eq!(result.mime_type.trim(), r#"application/pdf"#, "equals assertion failed");
    assert!(content.len() >= 5000, "expected length >= 5000, got {}", content.len());
    assert!(
        content.contains(r#"assembly"#) || content.contains(r#"register"#) || content.contains(r#"instruction"#),
        "expected to contain at least one of the specified values"
    );
    assert_eq!(result.metadata.format_type.trim(), r#"pdf"#, "equals assertion failed");
}

#[tokio::test]
async fn test_pdf_bayesian_data_analysis() {
    // Bayesian data analysis textbook PDF with large content volume.
    let path = String::new();
    let mime_type = None;
    let config = None;
    let result = extract_file(&path, &mime_type, config).await.expect("should succeed");
    let content = result.content.as_deref().unwrap_or("");
    assert_eq!(result.mime_type.trim(), r#"application/pdf"#, "equals assertion failed");
    assert!(
        content.len() >= 10000,
        "expected length >= 10000, got {}",
        content.len()
    );
    assert!(
        content.contains(r#"Bayesian"#) || content.contains(r#"probability"#) || content.contains(r#"distribution"#),
        "expected to contain at least one of the specified values"
    );
    assert_eq!(result.metadata.format_type.trim(), r#"pdf"#, "equals assertion failed");
}

#[tokio::test]
async fn test_pdf_code_and_formula() {
    // PDF containing code snippets and formulas should retain substantial content.
    let path = String::new();
    let mime_type = None;
    let config = None;
    let result = extract_file(&path, &mime_type, config).await.expect("should succeed");
    assert_eq!(result.mime_type.trim(), r#"application/pdf"#, "equals assertion failed");
    assert!(
        result.content.len() >= 100,
        "expected length >= 100, got {}",
        result.content.len()
    );
}

#[tokio::test]
async fn test_pdf_deep_learning() {
    // Deep learning textbook PDF to ensure long-form extraction quality.
    let path = String::new();
    let mime_type = None;
    let config = None;
    let result = extract_file(&path, &mime_type, config).await.expect("should succeed");
    let content = result.content.as_deref().unwrap_or("");
    assert_eq!(result.mime_type.trim(), r#"application/pdf"#, "equals assertion failed");
    assert!(content.len() >= 1000, "expected length >= 1000, got {}", content.len());
    assert!(
        content.contains(r#"neural"#) || content.contains(r#"network"#) || content.contains(r#"deep learning"#),
        "expected to contain at least one of the specified values"
    );
    assert_eq!(result.metadata.format_type.trim(), r#"pdf"#, "equals assertion failed");
}

#[tokio::test]
async fn test_pdf_embedded_images() {
    // PDF with embedded images should extract text and tables when present.
    let path = String::new();
    let mime_type = None;
    let config = None;
    let result = extract_file(&path, &mime_type, config).await.expect("should succeed");
    assert_eq!(result.mime_type.trim(), r#"application/pdf"#, "equals assertion failed");
    assert!(
        result.content.len() >= 50,
        "expected length >= 50, got {}",
        result.content.len()
    );
    assert!(
        result.tables.len() >= 0,
        "expected at least 0 elements, got {}",
        result.tables.len()
    );
}

#[tokio::test]
async fn test_pdf_google_doc() {
    // Google Docs exported PDF to verify conversion fidelity.
    let path = String::new();
    let mime_type = None;
    let config = None;
    let result = extract_file(&path, &mime_type, config).await.expect("should succeed");
    assert_eq!(result.mime_type.trim(), r#"application/pdf"#, "equals assertion failed");
    assert!(
        result.content.len() >= 50,
        "expected length >= 50, got {}",
        result.content.len()
    );
    assert_eq!(result.metadata.format_type.trim(), r#"pdf"#, "equals assertion failed");
}

#[tokio::test]
async fn test_pdf_large_ciml() {
    // Large machine learning textbook PDF to stress extraction length.
    let path = String::new();
    let mime_type = None;
    let config = None;
    let result = extract_file(&path, &mime_type, config).await.expect("should succeed");
    let content = result.content.as_deref().unwrap_or("");
    assert_eq!(result.mime_type.trim(), r#"application/pdf"#, "equals assertion failed");
    assert!(
        content.len() >= 10000,
        "expected length >= 10000, got {}",
        content.len()
    );
    assert!(
        content.contains(r#"machine learning"#) || content.contains(r#"algorithm"#) || content.contains(r#"training"#),
        "expected to contain at least one of the specified values"
    );
    assert_eq!(result.metadata.format_type.trim(), r#"pdf"#, "equals assertion failed");
}

#[tokio::test]
async fn test_pdf_non_english_german() {
    // German technical PDF to ensure non-ASCII content extraction.
    let path = String::new();
    let mime_type = None;
    let config = None;
    let result = extract_file(&path, &mime_type, config).await.expect("should succeed");
    let content = result.content.as_deref().unwrap_or("");
    assert_eq!(result.mime_type.trim(), r#"application/pdf"#, "equals assertion failed");
    assert!(content.len() >= 100, "expected length >= 100, got {}", content.len());
    assert!(
        content.contains(r#"Intel"#) || content.contains(r#"paging"#),
        "expected to contain at least one of the specified values"
    );
    assert_eq!(result.metadata.format_type.trim(), r#"pdf"#, "equals assertion failed");
}

#[tokio::test]
async fn test_pdf_right_to_left() {
    // Right-to-left language PDF to verify RTL extraction.
    let path = String::new();
    let mime_type = None;
    let config = None;
    let result = extract_file(&path, &mime_type, config).await.expect("should succeed");
    assert_eq!(result.mime_type.trim(), r#"application/pdf"#, "equals assertion failed");
    assert!(
        result.content.len() >= 50,
        "expected length >= 50, got {}",
        result.content.len()
    );
    assert_eq!(result.metadata.format_type.trim(), r#"pdf"#, "equals assertion failed");
}

#[tokio::test]
async fn test_pdf_simple_text() {
    // Simple text-heavy PDF should extract content without OCR or tables.
    let path = String::new();
    let mime_type = None;
    let config = None;
    let result = extract_file(&path, &mime_type, config).await.expect("should succeed");
    let content = result.content.as_deref().unwrap_or("");
    assert_eq!(result.mime_type.trim(), r#"application/pdf"#, "equals assertion failed");
    assert!(content.len() >= 50, "expected length >= 50, got {}", content.len());
    assert!(
        content.contains(r#"May 5, 2023"#)
            || content.contains(r#"To Whom it May Concern"#)
            || content.contains(r#"Mallori"#),
        "expected to contain at least one of the specified values"
    );
}

#[tokio::test]
async fn test_pdf_tables_large() {
    // Large PDF with extensive tables to stress table extraction.
    let path = String::new();
    let mime_type = None;
    let config = None;
    let result = extract_file(&path, &mime_type, config).await.expect("should succeed");
    assert_eq!(result.mime_type.trim(), r#"application/pdf"#, "equals assertion failed");
    assert!(
        result.content.len() >= 500,
        "expected length >= 500, got {}",
        result.content.len()
    );
}

#[tokio::test]
async fn test_pdf_tables_medium() {
    // Medium-sized PDF with multiple tables.
    let path = String::new();
    let mime_type = None;
    let config = None;
    let result = extract_file(&path, &mime_type, config).await.expect("should succeed");
    assert_eq!(result.mime_type.trim(), r#"application/pdf"#, "equals assertion failed");
    assert!(
        result.content.len() >= 100,
        "expected length >= 100, got {}",
        result.content.len()
    );
}

#[tokio::test]
async fn test_pdf_technical_stat_learning() {
    // Technical statistical learning PDF requiring substantial extraction.
    let path = String::new();
    let mime_type = None;
    let config = None;
    let result = extract_file(&path, &mime_type, config).await.expect("should succeed");
    let content = result.content.as_deref().unwrap_or("");
    assert_eq!(result.mime_type.trim(), r#"application/pdf"#, "equals assertion failed");
    assert!(
        content.len() >= 10000,
        "expected length >= 10000, got {}",
        content.len()
    );
    assert!(
        content.contains(r#"statistical"#) || content.contains(r#"regression"#) || content.contains(r#"learning"#),
        "expected to contain at least one of the specified values"
    );
    assert_eq!(result.metadata.format_type.trim(), r#"pdf"#, "equals assertion failed");
}
