// This file is auto-generated by alef. DO NOT EDIT.
//! E2e tests for category: smoke

use kreuzberg::extract_file;

#[tokio::test]
async fn test_smoke_cache_namespace() {
    // Smoke test: Extraction with cache namespace and TTL configuration
    let path = String::new();
    let mime_type = None;
    let config = None;
    let result = extract_file(&path, &mime_type, config).await.expect("should succeed");
    assert_eq!(result.mime_type.trim(), r#"text/plain"#, "equals assertion failed");
    assert!(
        result.content.len() >= 5,
        "expected length >= 5, got {}",
        result.content.len()
    );
}

#[tokio::test]
async fn test_smoke_docx_basic() {
    // Smoke test: DOCX with formatted text
    let path = String::new();
    let mime_type = None;
    let config = None;
    let result = extract_file(&path, &mime_type, config).await.expect("should succeed");
    let content = result.content.as_deref().unwrap_or("");
    assert_eq!(
        result.mime_type.trim(),
        r#"application/vnd.openxmlformats-officedocument.wordprocessingml.document"#,
        "equals assertion failed"
    );
    assert!(content.len() >= 20, "expected length >= 20, got {}", content.len());
    assert!(
        content.contains(r#"Lorem"#)
            || content.contains(r#"ipsum"#)
            || content.contains(r#"document"#)
            || content.contains(r#"text"#),
        "expected to contain at least one of the specified values"
    );
}

#[tokio::test]
async fn test_smoke_html_basic() {
    // Smoke test: HTML table extraction
    let path = String::new();
    let mime_type = None;
    let config = None;
    let result = extract_file(&path, &mime_type, config).await.expect("should succeed");
    let content = result.content.as_deref().unwrap_or("");
    assert_eq!(result.mime_type.trim(), r#"text/html"#, "equals assertion failed");
    assert!(content.len() >= 10, "expected length >= 10, got {}", content.len());
    assert!(
        content.contains(r#"Sample Data Table"#)
            || content.contains(r#"Laptop"#)
            || content.contains(r#"Electronics"#)
            || content.contains(r#"Product"#),
        "expected to contain at least one of the specified values"
    );
}

#[tokio::test]
async fn test_smoke_json_basic() {
    // Smoke test: JSON file extraction
    let path = String::new();
    let mime_type = None;
    let config = None;
    let result = extract_file(&path, &mime_type, config).await.expect("should succeed");
    assert_eq!(
        result.mime_type.trim(),
        r#"application/json"#,
        "equals assertion failed"
    );
    assert!(
        result.content.len() >= 5,
        "expected length >= 5, got {}",
        result.content.len()
    );
}

#[tokio::test]
async fn test_smoke_pdf_basic() {
    // Smoke test: PDF with simple text extraction
    let path = String::new();
    let mime_type = None;
    let config = None;
    let result = extract_file(&path, &mime_type, config).await.expect("should succeed");
    let content = result.content.as_deref().unwrap_or("");
    assert_eq!(result.mime_type.trim(), r#"application/pdf"#, "equals assertion failed");
    assert!(content.len() >= 50, "expected length >= 50, got {}", content.len());
    assert!(
        content.contains(r#"May 5, 2023"#) || content.contains(r#"To Whom it May Concern"#),
        "expected to contain at least one of the specified values"
    );
}

#[tokio::test]
async fn test_smoke_txt_basic() {
    // Smoke test: Plain text file
    let path = String::new();
    let mime_type = None;
    let config = None;
    let result = extract_file(&path, &mime_type, config).await.expect("should succeed");
    assert_eq!(result.mime_type.trim(), r#"text/plain"#, "equals assertion failed");
    assert!(
        result.content.len() >= 5,
        "expected length >= 5, got {}",
        result.content.len()
    );
}

#[tokio::test]
async fn test_smoke_xlsx_basic() {
    // Smoke test: XLSX with basic spreadsheet data including tables
    let path = String::new();
    let mime_type = None;
    let config = None;
    let result = extract_file(&path, &mime_type, config).await.expect("should succeed");
    let content = result.content.as_deref().unwrap_or("");
    assert_eq!(
        result.mime_type.trim(),
        r#"application/vnd.openxmlformats-officedocument.spreadsheetml.sheet"#,
        "equals assertion failed"
    );
    assert!(content.len() >= 100, "expected length >= 100, got {}", content.len());
    assert!(
        format!("{:?}", content).contains(r#"Team"#),
        "expected to contain: {}",
        r#"Team"#
    );
    assert!(
        format!("{:?}", content).contains(r#"Location"#),
        "expected to contain: {}",
        r#"Location"#
    );
    assert!(
        format!("{:?}", content).contains(r#"Stanley Cups"#),
        "expected to contain: {}",
        r#"Stanley Cups"#
    );
    assert!(
        format!("{:?}", content).contains(r#"Blues"#),
        "expected to contain: {}",
        r#"Blues"#
    );
    assert!(
        format!("{:?}", content).contains(r#"Flyers"#),
        "expected to contain: {}",
        r#"Flyers"#
    );
    assert!(
        format!("{:?}", content).contains(r#"Maple Leafs"#),
        "expected to contain: {}",
        r#"Maple Leafs"#
    );
    assert!(
        format!("{:?}", content).contains(r#"STL"#),
        "expected to contain: {}",
        r#"STL"#
    );
    assert!(
        format!("{:?}", content).contains(r#"PHI"#),
        "expected to contain: {}",
        r#"PHI"#
    );
    assert!(
        format!("{:?}", content).contains(r#"TOR"#),
        "expected to contain: {}",
        r#"TOR"#
    );
    assert!(
        result.tables.len() >= 1,
        "expected at least 1 elements, got {}",
        result.tables.len()
    );
    assert!(result.metadata.sheet_count >= 2, "expected >= 2");
    assert!(
        format!("{:?}", result.metadata.sheet_names).contains(r#"Stanley Cups"#),
        "expected to contain: {}",
        r#"Stanley Cups"#
    );
}
