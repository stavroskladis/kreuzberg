name: CI Go

on:
  push:
    branches: [main]
    paths:
      - 'packages/go/**'
      - 'crates/**'
      - 'scripts/ci/go/**'
      - '.github/actions/install-system-deps/**'
      - '.github/actions/setup-rust/**'
      - '.github/actions/cache-pdfium/**'
      - '.github/actions/download-pdfium/**'
      - '.github/actions/setup-onnx-runtime/**'
      - '.github/workflows/ci-go.yaml'
      - 'Cargo.toml'
      - 'Cargo.lock'
      - '.cargo/config.toml'
  pull_request:
    branches: [main]
    paths:
      - 'packages/go/**'
      - 'crates/**'
      - 'scripts/ci/go/**'
      - '.github/actions/install-system-deps/**'
      - '.github/actions/setup-rust/**'
      - '.github/actions/cache-pdfium/**'
      - '.github/actions/download-pdfium/**'
      - '.github/actions/setup-onnx-runtime/**'
      - '.github/workflows/ci-go.yaml'
      - 'Cargo.toml'
      - 'Cargo.lock'
      - '.cargo/config.toml'

concurrency:
  group: ci-go-${{ github.workflow }}-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: true

env:
  CARGO_TERM_COLOR: always
  CARGO_INCREMENTAL: 0
  CARGO_PROFILE_DEV_DEBUG: 0
  RUST_BACKTRACE: short
  RUST_MIN_STACK: 16777216
  PDFIUM_VERSION: "7529"
  ORT_VERSION: "1.23.2"
  MACOSX_DEPLOYMENT_TARGET: "14.0"

jobs:
  build-go:
    name: Go Bindings (${{ matrix.os }})
    timeout-minutes: 60
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, macos-latest, windows-latest]
    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/checkout@v6

      - name: Install system dependencies
        uses: ./.github/actions/install-system-deps

      - name: Setup Rust
        uses: ./.github/actions/setup-rust
        with:
          cache-key-prefix: go-${{ matrix.os }}
          # sccache is disabled on Windows GNU because it does not support MinGW gcc (zstd-sys build fails)
          use-sccache: ${{ matrix.os != 'windows-latest' }}
          target: ${{ matrix.os == 'windows-latest' && 'x86_64-pc-windows-gnu' || '' }}

      - name: Setup Go
        uses: actions/setup-go@v6
        with:
          go-version: '1.25'
          cache-dependency-path: packages/go/go.sum

      - name: Cache PDFium
        uses: ./.github/actions/cache-pdfium
        with:
          pdfium-version: ${{ env.PDFIUM_VERSION }}

      - name: Download PDFium
        uses: ./.github/actions/download-pdfium
        with:
          pdfium-version: ${{ env.PDFIUM_VERSION }}

      - name: Setup ONNX Runtime
        uses: ./.github/actions/setup-onnx-runtime
        with:
          ort-version: ${{ env.ORT_VERSION }}

      - name: Setup MSYS2 MinGW toolchain (Windows only)
        if: matrix.os == 'windows-latest'
        uses: msys2/setup-msys2@v2
        with:
          msystem: UCRT64
          update: true
          install: >-
            mingw-w64-ucrt-x86_64-gcc
            mingw-w64-ucrt-x86_64-cmake
            mingw-w64-ucrt-x86_64-pkg-config
            mingw-w64-ucrt-x86_64-nasm
            mingw-w64-ucrt-x86_64-zstd

      - name: Verify MSYS2 toolchain and add to PATH (Windows only)
        if: matrix.os == 'windows-latest'
        shell: pwsh
        run: |
          # Verify MSYS2 installation
          if (-not (Test-Path "C:\msys64\ucrt64\bin")) {
            throw "MSYS2 UCRT64 bin directory not found"
          }

          # List installed executables for debugging
          Write-Host "Installed MSYS2 executables:"
          Get-ChildItem "C:\msys64\ucrt64\bin" -Filter "*.exe" | Select-Object -First 20 | ForEach-Object { Write-Host "  $($_.Name)" }

          # Verify required tools
          $requiredTools = @("gcc.exe", "ar.exe", "ranlib.exe", "pkg-config.exe", "nasm.exe")
          $missing = @()
          foreach ($tool in $requiredTools) {
            if (-not (Test-Path "C:\msys64\ucrt64\bin\$tool")) {
              $missing += $tool
            }
          }

          if ($missing.Count -gt 0) {
            Write-Host "ERROR: Missing tools: $($missing -join ', ')"
            Write-Host "Running pacman to ensure all packages are installed..."

            # Run pacman in MSYS2 shell to ensure packages are installed
            C:\msys64\usr\bin\bash.exe -lc "pacman -S --needed --noconfirm mingw-w64-ucrt-x86_64-gcc mingw-w64-ucrt-x86_64-binutils mingw-w64-ucrt-x86_64-pkg-config mingw-w64-ucrt-x86_64-nasm"

            # Verify again
            foreach ($tool in $missing) {
              if (-not (Test-Path "C:\msys64\ucrt64\bin\$tool")) {
                throw "Failed to install $tool"
              }
            }
          }

          # Add UCRT64 bin to PATH for subsequent steps
          Add-Content -Path $env:GITHUB_PATH -Value "C:\msys64\ucrt64\bin"
          Write-Host "Added C:\msys64\ucrt64\bin to PATH"

      - name: Build FFI library
        run: scripts/ci/go/build-ffi.ps1
        shell: pwsh

      - name: Build Go bindings
        run: scripts/ci/go/build-bindings.ps1
        shell: pwsh

      - name: Upload FFI library
        uses: actions/upload-artifact@v5
        with:
          name: go-ffi-lib-${{ matrix.os }}
          path: |
            target/release/libkreuzberg_ffi.*
            target/x86_64-pc-windows-gnu/release/libkreuzberg_ffi.*
          retention-days: 7

      - name: Cleanup Rust cache
        if: always()
        uses: ./.github/actions/cleanup-rust-cache

  test-go:
    name: Go Tests (${{ matrix.os }})
    needs: build-go
    timeout-minutes: 60
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, macos-latest, windows-latest]
    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/checkout@v6

      - name: Install system dependencies
        uses: ./.github/actions/install-system-deps

      - name: Setup OpenSSL
        uses: ./.github/actions/setup-openssl

      - name: Setup Rust
        uses: ./.github/actions/setup-rust
        with:
          cache-key-prefix: go-tests-${{ matrix.os }}
          # sccache is disabled on Windows GNU because it does not support MinGW gcc (zstd-sys build fails)
          use-sccache: ${{ matrix.os != 'windows-latest' }}
          target: ${{ matrix.os == 'windows-latest' && 'x86_64-pc-windows-gnu' || '' }}

      - name: Setup MSYS2 MinGW toolchain (Windows only)
        if: matrix.os == 'windows-latest'
        uses: msys2/setup-msys2@v2
        with:
          msystem: UCRT64
          update: true
          install: >-
            mingw-w64-ucrt-x86_64-gcc
            mingw-w64-ucrt-x86_64-cmake
            mingw-w64-ucrt-x86_64-pkg-config
            mingw-w64-ucrt-x86_64-nasm
            mingw-w64-ucrt-x86_64-zstd

      - name: Verify MSYS2 toolchain and add to PATH (Windows only)
        if: matrix.os == 'windows-latest'
        shell: pwsh
        run: |
          # Verify MSYS2 installation
          if (-not (Test-Path "C:\msys64\ucrt64\bin")) {
            throw "MSYS2 UCRT64 bin directory not found"
          }

          # List installed executables for debugging
          Write-Host "Installed MSYS2 executables:"
          Get-ChildItem "C:\msys64\ucrt64\bin" -Filter "*.exe" | Select-Object -First 20 | ForEach-Object { Write-Host "  $($_.Name)" }

          # Verify required tools
          $requiredTools = @("gcc.exe", "ar.exe", "ranlib.exe", "pkg-config.exe", "nasm.exe")
          $missing = @()
          foreach ($tool in $requiredTools) {
            if (-not (Test-Path "C:\msys64\ucrt64\bin\$tool")) {
              $missing += $tool
            }
          }

          if ($missing.Count -gt 0) {
            Write-Host "ERROR: Missing tools: $($missing -join ', ')"
            Write-Host "Running pacman to ensure all packages are installed..."

            # Run pacman in MSYS2 shell to ensure packages are installed
            C:\msys64\usr\bin\bash.exe -lc "pacman -S --needed --noconfirm mingw-w64-ucrt-x86_64-gcc mingw-w64-ucrt-x86_64-binutils mingw-w64-ucrt-x86_64-pkg-config mingw-w64-ucrt-x86_64-nasm"

            # Verify again
            foreach ($tool in $missing) {
              if (-not (Test-Path "C:\msys64\ucrt64\bin\$tool")) {
                throw "Failed to install $tool"
              }
            }
          }

          # Add UCRT64 bin to PATH for subsequent steps
          Add-Content -Path $env:GITHUB_PATH -Value "C:\msys64\ucrt64\bin"
          Write-Host "Added C:\msys64\ucrt64\bin to PATH"

      - name: Setup Go
        uses: actions/setup-go@v6
        with:
          go-version: "1.25"
          cache-dependency-path: packages/go/go.sum

      - name: Cache PDFium
        uses: ./.github/actions/cache-pdfium
        with:
          pdfium-version: ${{ env.PDFIUM_VERSION }}

      - name: Download PDFium
        uses: ./.github/actions/download-pdfium
        with:
          pdfium-version: ${{ env.PDFIUM_VERSION }}

      - name: Stage Pdfium runtime
        uses: ./.github/actions/stage-pdfium-runtime
        with:
          destination: target/release

      - name: Setup ONNX Runtime
        uses: ./.github/actions/setup-onnx-runtime
        with:
          ort-version: ${{ env.ORT_VERSION }}

      - name: Download FFI library
        uses: actions/download-artifact@v6
        with:
          name: go-ffi-lib-${{ matrix.os }}
          path: target/release/

      - name: Make FFI library executable
        run: chmod +x target/release/libkreuzberg_ffi.* || true

      - name: Run Go tests
        run: scripts/ci/go/run-tests.sh
        shell: bash

      - name: Install Task
        uses: ./.github/actions/install-task

      - name: Run E2E tests
        run: |
          export LD_LIBRARY_PATH="${PWD}/target/release:${LD_LIBRARY_PATH:-}"
          export DYLD_LIBRARY_PATH="${PWD}/target/release:${DYLD_LIBRARY_PATH:-}"
          export DYLD_FALLBACK_LIBRARY_PATH="${PWD}/target/release:${DYLD_FALLBACK_LIBRARY_PATH:-}"
          export TESSDATA_PREFIX=/usr/share/tesseract-ocr/5/tessdata
          task e2e:go:test
