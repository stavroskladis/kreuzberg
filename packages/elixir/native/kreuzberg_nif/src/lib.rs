// This file is auto-generated by alef. DO NOT EDIT.
// Re-generate with: alef generate

#[derive(Debug, Clone, rustler::NifStruct)]
#[module = "Kreuzberg.SupportedFormat"]
pub struct SupportedFormat {
    pub extension: String,
    pub mime_type: String,
}

#[derive(Debug, Clone, Default, rustler::NifMap)]
pub struct LlmUsage {
    pub model: String,
    pub source: String,
    pub input_tokens: Option<u64>,
    pub output_tokens: Option<u64>,
    pub total_tokens: Option<u64>,
    pub estimated_cost: Option<f64>,
    pub finish_reason: Option<String>,
}

impl LlmUsage {
    pub fn new(opts: std::collections::HashMap<String, rustler::Term>) -> Self {
        Self {
            model: opts.get("model").and_then(|t| t.decode().ok()).unwrap_or_default(),
            source: opts.get("source").and_then(|t| t.decode().ok()).unwrap_or_default(),
            input_tokens: opts.get("input_tokens").and_then(|t| t.decode().ok()),
            output_tokens: opts.get("output_tokens").and_then(|t| t.decode().ok()),
            total_tokens: opts.get("total_tokens").and_then(|t| t.decode().ok()),
            estimated_cost: opts.get("estimated_cost").and_then(|t| t.decode().ok()),
            finish_reason: opts.get("finish_reason").and_then(|t| t.decode().ok()),
        }
    }
}

#[rustler::nif]
pub fn is_valid_format_field(field: String) -> bool {
    kreuzberg::is_valid_format_field(&field)
}

#[rustler::nif]
pub fn detect_mime_type(_path: String, _check_exists: bool) -> Result<String, String> {
    Err(String::from("Not implemented: detect_mime_type"))
}

#[rustler::nif]
pub fn validate_mime_type(mime_type: String) -> Result<String, String> {
    let result = kreuzberg::validate_mime_type(&mime_type).map_err(|e| e.to_string())?;
    Ok(result)
}

#[rustler::nif]
pub fn detect_mime_type_from_bytes(content: Vec<u8>) -> Result<String, String> {
    let result = kreuzberg::detect_mime_type_from_bytes(&content).map_err(|e| e.to_string())?;
    Ok(result)
}

#[rustler::nif]
pub fn get_extensions_for_mime(mime_type: String) -> Result<Vec<String>, String> {
    let result = kreuzberg::get_extensions_for_mime(&mime_type).map_err(|e| e.to_string())?;
    Ok(result)
}

#[rustler::nif]
pub fn list_supported_formats() -> Vec<SupportedFormat> {
    kreuzberg::list_supported_formats()
        .into_iter()
        .map(Into::into)
        .collect()
}

impl From<SupportedFormat> for kreuzberg::SupportedFormat {
    fn from(val: SupportedFormat) -> Self {
        Self {
            extension: val.extension,
            mime_type: val.mime_type,
        }
    }
}

impl From<kreuzberg::SupportedFormat> for SupportedFormat {
    fn from(val: kreuzberg::SupportedFormat) -> Self {
        Self {
            extension: val.extension,
            mime_type: val.mime_type,
        }
    }
}

impl From<LlmUsage> for kreuzberg::LlmUsage {
    fn from(val: LlmUsage) -> Self {
        Self {
            model: val.model,
            source: val.source,
            input_tokens: val.input_tokens,
            output_tokens: val.output_tokens,
            total_tokens: val.total_tokens,
            estimated_cost: val.estimated_cost,
            finish_reason: val.finish_reason,
        }
    }
}

impl From<kreuzberg::LlmUsage> for LlmUsage {
    fn from(val: kreuzberg::LlmUsage) -> Self {
        Self {
            model: val.model,
            source: val.source,
            input_tokens: val.input_tokens,
            output_tokens: val.output_tokens,
            total_tokens: val.total_tokens,
            estimated_cost: val.estimated_cost,
            finish_reason: val.finish_reason,
        }
    }
}

/// Convert a `kreuzberg::error::KreuzbergError` error to a Rustler error string.
#[allow(dead_code)]
fn kreuzberg_error_to_rustler_err(e: kreuzberg::error::KreuzbergError) -> String {
    e.to_string()
}

rustler::init!("Elixir.Kreuzberg.Native");
