// DO NOT EDIT - auto-generated by alef
package dev.kreuzberg;

import java.lang.foreign.MemorySegment;

/**
 * PST file extractor.
 * 
 * Supports: .pst (Microsoft Outlook Personal Folders)
 */
public class PstExtractor implements AutoCloseable {
    private final MemorySegment handle;

    PstExtractor(MemorySegment handle) {
        this.handle = handle;
    }

    MemorySegment handle() {
        return this.handle;
    }

    @Override
    public void close() {
        if (handle != null && !handle.equals(MemorySegment.NULL)) {
            try {
                NativeLib.KREUZBERG_PST_EXTRACTOR_FREE.invoke(handle);
            } catch (Throwable e) {
                throw new RuntimeException("Failed to free PstExtractor: " + e.getMessage(), e);
            }
        }
    }
}
