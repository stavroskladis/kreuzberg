// DO NOT EDIT - auto-generated by alef
package dev.kreuzberg;

import java.lang.foreign.MemorySegment;

/**
 * Structured data extractor supporting JSON, JSONL/NDJSON, YAML, and TOML.
 */
public class StructuredExtractor implements AutoCloseable {
    private final MemorySegment handle;

    StructuredExtractor(MemorySegment handle) {
        this.handle = handle;
    }

    MemorySegment handle() {
        return this.handle;
    }

    @Override
    public void close() {
        if (handle != null && !handle.equals(MemorySegment.NULL)) {
            try {
                NativeLib.KREUZBERG_STRUCTURED_EXTRACTOR_FREE.invoke(handle);
            } catch (Throwable e) {
                throw new RuntimeException("Failed to free StructuredExtractor: " + e.getMessage(), e);
            }
        }
    }
}
