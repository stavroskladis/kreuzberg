// DO NOT EDIT - auto-generated by alef
package dev.kreuzberg;

import java.lang.foreign.MemorySegment;

/**
 * High-performance DOCX extractor.
 * 
 * This extractor provides:
 * - Fast text extraction via streaming XML parsing
 * - Comprehensive metadata extraction (core.xml, app.xml, custom.xml)
 */
public class DocxExtractor implements AutoCloseable {
    private final MemorySegment handle;

    DocxExtractor(MemorySegment handle) {
        this.handle = handle;
    }

    MemorySegment handle() {
        return this.handle;
    }

    @Override
    public void close() {
        if (handle != null && !handle.equals(MemorySegment.NULL)) {
            try {
                NativeLib.KREUZBERG_DOCX_EXTRACTOR_FREE.invoke(handle);
            } catch (Throwable e) {
                throw new RuntimeException("Failed to free DocxExtractor: " + e.getMessage(), e);
            }
        }
    }
}
