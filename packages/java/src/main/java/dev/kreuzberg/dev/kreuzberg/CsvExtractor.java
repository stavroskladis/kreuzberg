// DO NOT EDIT - auto-generated by alef
package dev.kreuzberg;

import java.lang.foreign.MemorySegment;

/**
 * CSV/TSV extractor with proper field parsing.
 * 
 * Replaces raw text passthrough with structured CSV parsing,
 * producing space-separated text output and populated `tables` field.
 */
public class CsvExtractor implements AutoCloseable {
    private final MemorySegment handle;

    CsvExtractor(MemorySegment handle) {
        this.handle = handle;
    }

    MemorySegment handle() {
        return this.handle;
    }

    @Override
    public void close() {
        if (handle != null && !handle.equals(MemorySegment.NULL)) {
            try {
                NativeLib.KREUZBERG_CSV_EXTRACTOR_FREE.invoke(handle);
            } catch (Throwable e) {
                throw new RuntimeException("Failed to free CsvExtractor: " + e.getMessage(), e);
            }
        }
    }
}
