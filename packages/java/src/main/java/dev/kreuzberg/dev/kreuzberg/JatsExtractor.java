// DO NOT EDIT - auto-generated by alef
package dev.kreuzberg;

import java.lang.foreign.MemorySegment;

/**
 * JATS document extractor.
 * 
 * Supports JATS (Journal Article Tag Suite) XML documents in various versions,
 * handling both the full article structure and minimal JATS subsets.
 */
public class JatsExtractor implements AutoCloseable {
    private final MemorySegment handle;

    JatsExtractor(MemorySegment handle) {
        this.handle = handle;
    }

    MemorySegment handle() {
        return this.handle;
    }

    @Override
    public void close() {
        if (handle != null && !handle.equals(MemorySegment.NULL)) {
            try {
                NativeLib.KREUZBERG_JATS_EXTRACTOR_FREE.invoke(handle);
            } catch (Throwable e) {
                throw new RuntimeException("Failed to free JatsExtractor: " + e.getMessage(), e);
            }
        }
    }
}
