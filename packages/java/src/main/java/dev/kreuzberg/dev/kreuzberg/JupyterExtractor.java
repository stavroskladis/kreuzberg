// DO NOT EDIT - auto-generated by alef
package dev.kreuzberg;

import java.lang.foreign.MemorySegment;

/**
 * Jupyter Notebook extractor.
 * 
 * Extracts content from Jupyter notebook JSON files, including:
 * - Notebook metadata (kernel, language, nbformat version)
 * - Cell content (code and markdown)
 * - Cell outputs (text, HTML, etc.)
 * - Cell-level metadata (tags, execution counts)
 */
public class JupyterExtractor implements AutoCloseable {
    private final MemorySegment handle;

    JupyterExtractor(MemorySegment handle) {
        this.handle = handle;
    }

    MemorySegment handle() {
        return this.handle;
    }

    @Override
    public void close() {
        if (handle != null && !handle.equals(MemorySegment.NULL)) {
            try {
                NativeLib.KREUZBERG_JUPYTER_EXTRACTOR_FREE.invoke(handle);
            } catch (Throwable e) {
                throw new RuntimeException("Failed to free JupyterExtractor: " + e.getMessage(), e);
            }
        }
    }
}
