// DO NOT EDIT - auto-generated by alef
package dev.kreuzberg;

import java.lang.foreign.MemorySegment;

/**
 * Apple Numbers spreadsheet extractor.
 * 
 * Supports `.numbers` files (modern iWork format, 2013+).
 * 
 * Extracts cell string values and sheet names from the IWA container:
 * ZIP → Snappy → protobuf text fields. Output is formatted as plain text
 * with one text token per line (representing cell values and labels).
 */
public class NumbersExtractor implements AutoCloseable {
    private final MemorySegment handle;

    NumbersExtractor(MemorySegment handle) {
        this.handle = handle;
    }

    MemorySegment handle() {
        return this.handle;
    }

    @Override
    public void close() {
        if (handle != null && !handle.equals(MemorySegment.NULL)) {
            try {
                NativeLib.KREUZBERG_NUMBERS_EXTRACTOR_FREE.invoke(handle);
            } catch (Throwable e) {
                throw new RuntimeException("Failed to free NumbersExtractor: " + e.getMessage(), e);
            }
        }
    }
}
