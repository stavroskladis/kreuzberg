// DO NOT EDIT - auto-generated by alef
package dev.kreuzberg;

import java.lang.foreign.MemorySegment;

/**
 * Image extractor for various image formats.
 * 
 * Supports: PNG, JPEG, WebP, BMP, TIFF, GIF.
 * Extracts dimensions, format, and EXIF metadata.
 * Optionally runs OCR when configured.
 * When layout detection is also enabled, uses per-region OCR with
 * markdown formatting based on detected layout classes.
 */
public class ImageExtractor implements AutoCloseable {
    private final MemorySegment handle;

    ImageExtractor(MemorySegment handle) {
        this.handle = handle;
    }

    MemorySegment handle() {
        return this.handle;
    }

    @Override
    public void close() {
        if (handle != null && !handle.equals(MemorySegment.NULL)) {
            try {
                NativeLib.KREUZBERG_IMAGE_EXTRACTOR_FREE.invoke(handle);
            } catch (Throwable e) {
                throw new RuntimeException("Failed to free ImageExtractor: " + e.getMessage(), e);
            }
        }
    }
}
