// DO NOT EDIT - auto-generated by alef
package dev.kreuzberg;

import java.lang.foreign.MemorySegment;

/**
 * HTML document extractor using html-to-markdown.
 */
public class HtmlExtractor implements AutoCloseable {
    private final MemorySegment handle;

    HtmlExtractor(MemorySegment handle) {
        this.handle = handle;
    }

    MemorySegment handle() {
        return this.handle;
    }

    @Override
    public void close() {
        if (handle != null && !handle.equals(MemorySegment.NULL)) {
            try {
                NativeLib.KREUZBERG_HTML_EXTRACTOR_FREE.invoke(handle);
            } catch (Throwable e) {
                throw new RuntimeException("Failed to free HtmlExtractor: " + e.getMessage(), e);
            }
        }
    }
}
