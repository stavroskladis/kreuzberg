// DO NOT EDIT - auto-generated by alef
package dev.kreuzberg;

import java.lang.foreign.MemorySegment;

/**
 * Registry for document extractor plugins.
 * 
 * Manages extractors with MIME type and priority-based selection.
 * 
 * # Thread Safety
 * 
 * The registry is thread-safe and can be accessed concurrently from multiple threads.
 */
public class DocumentExtractorRegistry implements AutoCloseable {
    private final MemorySegment handle;

    DocumentExtractorRegistry(MemorySegment handle) {
        this.handle = handle;
    }

    MemorySegment handle() {
        return this.handle;
    }

    @Override
    public void close() {
        if (handle != null && !handle.equals(MemorySegment.NULL)) {
            try {
                NativeLib.KREUZBERG_DOCUMENT_EXTRACTOR_REGISTRY_FREE.invoke(handle);
            } catch (Throwable e) {
                throw new RuntimeException("Failed to free DocumentExtractorRegistry: " + e.getMessage(), e);
            }
        }
    }
}
