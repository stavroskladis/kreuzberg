// DO NOT EDIT - auto-generated by alef
package dev.kreuzberg;

import java.lang.foreign.MemorySegment;

/**
 * PDF document extractor using pypdfium2 and playa-pdf.
 */
public class PdfExtractor implements AutoCloseable {
    private final MemorySegment handle;

    PdfExtractor(MemorySegment handle) {
        this.handle = handle;
    }

    MemorySegment handle() {
        return this.handle;
    }

    @Override
    public void close() {
        if (handle != null && !handle.equals(MemorySegment.NULL)) {
            try {
                NativeLib.KREUZBERG_PDF_EXTRACTOR_FREE.invoke(handle);
            } catch (Throwable e) {
                throw new RuntimeException("Failed to free PdfExtractor: " + e.getMessage(), e);
            }
        }
    }
}
