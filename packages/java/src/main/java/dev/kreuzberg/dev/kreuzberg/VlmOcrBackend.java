// DO NOT EDIT - auto-generated by alef
package dev.kreuzberg;

import java.lang.foreign.MemorySegment;

/**
 * VLM-based OCR backend using liter-llm vision models.
 * 
 * This backend sends images to a vision language model (e.g., GPT-4o, Claude)
 * for text extraction, as an alternative to traditional OCR backends.
 */
public class VlmOcrBackend implements AutoCloseable {
    private final MemorySegment handle;

    VlmOcrBackend(MemorySegment handle) {
        this.handle = handle;
    }

    MemorySegment handle() {
        return this.handle;
    }

    @Override
    public void close() {
        if (handle != null && !handle.equals(MemorySegment.NULL)) {
            try {
                NativeLib.KREUZBERG_VLM_OCR_BACKEND_FREE.invoke(handle);
            } catch (Throwable e) {
                throw new RuntimeException("Failed to free VlmOcrBackend: " + e.getMessage(), e);
            }
        }
    }
}
