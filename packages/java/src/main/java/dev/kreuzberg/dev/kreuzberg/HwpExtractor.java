// DO NOT EDIT - auto-generated by alef
package dev.kreuzberg;

import java.lang.foreign.MemorySegment;

/**
 * Extractor for Hangul Word Processor (.hwp) files.
 * 
 * Supports HWP 5.0 format, the standard document format in South Korea.
 */
public class HwpExtractor implements AutoCloseable {
    private final MemorySegment handle;

    HwpExtractor(MemorySegment handle) {
        this.handle = handle;
    }

    MemorySegment handle() {
        return this.handle;
    }

    @Override
    public void close() {
        if (handle != null && !handle.equals(MemorySegment.NULL)) {
            try {
                NativeLib.KREUZBERG_HWP_EXTRACTOR_FREE.invoke(handle);
            } catch (Throwable e) {
                throw new RuntimeException("Failed to free HwpExtractor: " + e.getMessage(), e);
            }
        }
    }
}
