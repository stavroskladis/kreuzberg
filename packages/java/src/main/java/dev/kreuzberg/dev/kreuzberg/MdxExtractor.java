// DO NOT EDIT - auto-generated by alef
package dev.kreuzberg;

import java.lang.foreign.MemorySegment;

/**
 * MDX extractor with JSX stripping and Markdown processing.
 * 
 * Strips MDX-specific syntax (imports, exports, JSX component tags,
 * inline expressions) and processes the remaining content as Markdown,
 * extracting metadata from YAML frontmatter and tables.
 */
public class MdxExtractor implements AutoCloseable {
    private final MemorySegment handle;

    MdxExtractor(MemorySegment handle) {
        this.handle = handle;
    }

    MemorySegment handle() {
        return this.handle;
    }

    @Override
    public void close() {
        if (handle != null && !handle.equals(MemorySegment.NULL)) {
            try {
                NativeLib.KREUZBERG_MDX_EXTRACTOR_FREE.invoke(handle);
            } catch (Throwable e) {
                throw new RuntimeException("Failed to free MdxExtractor: " + e.getMessage(), e);
            }
        }
    }
}
