// DO NOT EDIT - auto-generated by alef
package dev.kreuzberg;

import java.lang.foreign.MemorySegment;

/**
 * OPML format extractor.
 * 
 * Extracts outline structure and metadata from OPML documents using native Rust parsing.
 */
public class OpmlExtractor implements AutoCloseable {
    private final MemorySegment handle;

    OpmlExtractor(MemorySegment handle) {
        this.handle = handle;
    }

    MemorySegment handle() {
        return this.handle;
    }

    @Override
    public void close() {
        if (handle != null && !handle.equals(MemorySegment.NULL)) {
            try {
                NativeLib.KREUZBERG_OPML_EXTRACTOR_FREE.invoke(handle);
            } catch (Throwable e) {
                throw new RuntimeException("Failed to free OpmlExtractor: " + e.getMessage(), e);
            }
        }
    }
}
