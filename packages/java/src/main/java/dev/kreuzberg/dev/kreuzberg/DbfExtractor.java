// DO NOT EDIT - auto-generated by alef
package dev.kreuzberg;

import java.lang.foreign.MemorySegment;

/**
 * Extractor for dBASE (.dbf) files.
 * 
 * Reads all records and formats them as a markdown table with
 * column headers derived from field names.
 */
public class DbfExtractor implements AutoCloseable {
    private final MemorySegment handle;

    DbfExtractor(MemorySegment handle) {
        this.handle = handle;
    }

    MemorySegment handle() {
        return this.handle;
    }

    @Override
    public void close() {
        if (handle != null && !handle.equals(MemorySegment.NULL)) {
            try {
                NativeLib.KREUZBERG_DBF_EXTRACTOR_FREE.invoke(handle);
            } catch (Throwable e) {
                throw new RuntimeException("Failed to free DbfExtractor: " + e.getMessage(), e);
            }
        }
    }
}
