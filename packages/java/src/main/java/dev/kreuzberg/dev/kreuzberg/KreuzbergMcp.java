// DO NOT EDIT - auto-generated by alef
package dev.kreuzberg;

import java.lang.foreign.MemorySegment;

/**
 * Kreuzberg MCP server.
 * 
 * Provides document extraction capabilities via MCP tools.
 * 
 * The server loads a default extraction configuration from kreuzberg.toml/yaml/json
 * via discovery. Per-request OCR settings override the defaults.
 */
public class KreuzbergMcp implements AutoCloseable {
    private final MemorySegment handle;

    KreuzbergMcp(MemorySegment handle) {
        this.handle = handle;
    }

    MemorySegment handle() {
        return this.handle;
    }

    @Override
    public void close() {
        if (handle != null && !handle.equals(MemorySegment.NULL)) {
            try {
                NativeLib.KREUZBERG_KREUZBERG_MCP_FREE.invoke(handle);
            } catch (Throwable e) {
                throw new RuntimeException("Failed to free KreuzbergMcp: " + e.getMessage(), e);
            }
        }
    }
}
