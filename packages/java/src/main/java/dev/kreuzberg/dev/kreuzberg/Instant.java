// DO NOT EDIT - auto-generated by alef
package dev.kreuzberg;

import java.lang.foreign.MemorySegment;

/**
 * A platform-aware instant for measuring elapsed time.
 * 
 * On native targets this delegates to [`std::time::Instant`].
 * On `wasm32` targets it is a zero-cost no-op to avoid the `unreachable` trap.
 */
public class Instant implements AutoCloseable {
    private final MemorySegment handle;

    Instant(MemorySegment handle) {
        this.handle = handle;
    }

    MemorySegment handle() {
        return this.handle;
    }

    @Override
    public void close() {
        if (handle != null && !handle.equals(MemorySegment.NULL)) {
            try {
                NativeLib.KREUZBERG_INSTANT_FREE.invoke(handle);
            } catch (Throwable e) {
                throw new RuntimeException("Failed to free Instant: " + e.getMessage(), e);
            }
        }
    }
}
