// DO NOT EDIT - auto-generated by alef
package dev.kreuzberg;

import java.lang.foreign.MemorySegment;

/**
 * A [`tower::Layer`] that records service-level extraction metrics.
 */
public class MetricsLayer implements AutoCloseable {
    private final MemorySegment handle;

    MetricsLayer(MemorySegment handle) {
        this.handle = handle;
    }

    MemorySegment handle() {
        return this.handle;
    }

    @Override
    public void close() {
        if (handle != null && !handle.equals(MemorySegment.NULL)) {
            try {
                NativeLib.KREUZBERG_METRICS_LAYER_FREE.invoke(handle);
            } catch (Throwable e) {
                throw new RuntimeException("Failed to free MetricsLayer: " + e.getMessage(), e);
            }
        }
    }
}
