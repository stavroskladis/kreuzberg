// DO NOT EDIT - auto-generated by alef
package dev.kreuzberg;

import java.lang.foreign.MemorySegment;

/**
 * Registry for post-processor plugins.
 * 
 * Manages post-processors organized by processing stage.
 */
public class PostProcessorRegistry implements AutoCloseable {
    private final MemorySegment handle;

    PostProcessorRegistry(MemorySegment handle) {
        this.handle = handle;
    }

    MemorySegment handle() {
        return this.handle;
    }

    @Override
    public void close() {
        if (handle != null && !handle.equals(MemorySegment.NULL)) {
            try {
                NativeLib.KREUZBERG_POST_PROCESSOR_REGISTRY_FREE.invoke(handle);
            } catch (Throwable e) {
                throw new RuntimeException("Failed to free PostProcessorRegistry: " + e.getMessage(), e);
            }
        }
    }
}
