// DO NOT EDIT - auto-generated by alef
package dev.kreuzberg;

import java.lang.foreign.MemorySegment;

/**
 * PowerPoint presentation extractor.
 * 
 * Supports: .pptx, .pptm, .ppsx
 */
public class PptxExtractor implements AutoCloseable {
    private final MemorySegment handle;

    PptxExtractor(MemorySegment handle) {
        this.handle = handle;
    }

    MemorySegment handle() {
        return this.handle;
    }

    @Override
    public void close() {
        if (handle != null && !handle.equals(MemorySegment.NULL)) {
            try {
                NativeLib.KREUZBERG_PPTX_EXTRACTOR_FREE.invoke(handle);
            } catch (Throwable e) {
                throw new RuntimeException("Failed to free PptxExtractor: " + e.getMessage(), e);
            }
        }
    }
}
