// DO NOT EDIT - auto-generated by alef
package dev.kreuzberg;

import java.lang.foreign.MemorySegment;

/**
 * Org Mode document extractor.
 * 
 * Provides native Rust-based Org Mode extraction using the `org` library,
 * extracting structured content and metadata.
 */
public class OrgModeExtractor implements AutoCloseable {
    private final MemorySegment handle;

    OrgModeExtractor(MemorySegment handle) {
        this.handle = handle;
    }

    MemorySegment handle() {
        return this.handle;
    }

    @Override
    public void close() {
        if (handle != null && !handle.equals(MemorySegment.NULL)) {
            try {
                NativeLib.KREUZBERG_ORG_MODE_EXTRACTOR_FREE.invoke(handle);
            } catch (Throwable e) {
                throw new RuntimeException("Failed to free OrgModeExtractor: " + e.getMessage(), e);
            }
        }
    }
}
