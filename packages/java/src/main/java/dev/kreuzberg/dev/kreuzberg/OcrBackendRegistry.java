// DO NOT EDIT - auto-generated by alef
package dev.kreuzberg;

import java.lang.foreign.MemorySegment;

/**
 * Registry for OCR backend plugins.
 * 
 * Manages OCR backends with backend type and language-based selection.
 * 
 * # Thread Safety
 * 
 * The registry is thread-safe and can be accessed concurrently from multiple threads.
 * 
 * # Example
 * 
 * ```rust,no_run
 * use kreuzberg::plugins::registry::OcrBackendRegistry;
 * use std::sync::Arc;
 * 
 * let registry = OcrBackendRegistry::new();
 * // Register OCR backends
 * // registry.register(Arc::new(TesseractBackend::new()));
 * ```
 */
public class OcrBackendRegistry implements AutoCloseable {
    private final MemorySegment handle;

    OcrBackendRegistry(MemorySegment handle) {
        this.handle = handle;
    }

    MemorySegment handle() {
        return this.handle;
    }

    @Override
    public void close() {
        if (handle != null && !handle.equals(MemorySegment.NULL)) {
            try {
                NativeLib.KREUZBERG_OCR_BACKEND_REGISTRY_FREE.invoke(handle);
            } catch (Throwable e) {
                throw new RuntimeException("Failed to free OcrBackendRegistry: " + e.getMessage(), e);
            }
        }
    }
}
