// DO NOT EDIT - auto-generated by alef
package dev.kreuzberg;

import java.lang.foreign.MemorySegment;

/**
 * Native DOC extractor using OLE/CFB parsing.
 * 
 * This extractor handles Word 97-2003 binary (.doc) files without
 * requiring LibreOffice, providing ~50x faster extraction.
 */
public class DocExtractor implements AutoCloseable {
    private final MemorySegment handle;

    DocExtractor(MemorySegment handle) {
        this.handle = handle;
    }

    MemorySegment handle() {
        return this.handle;
    }

    @Override
    public void close() {
        if (handle != null && !handle.equals(MemorySegment.NULL)) {
            try {
                NativeLib.KREUZBERG_DOC_EXTRACTOR_FREE.invoke(handle);
            } catch (Throwable e) {
                throw new RuntimeException("Failed to free DocExtractor: " + e.getMessage(), e);
            }
        }
    }
}
