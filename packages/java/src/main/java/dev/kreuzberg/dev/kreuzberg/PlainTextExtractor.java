// DO NOT EDIT - auto-generated by alef
package dev.kreuzberg;

import java.lang.foreign.MemorySegment;

/**
 * Plain text extractor.
 * 
 * Extracts content from plain text files (.txt).
 */
public class PlainTextExtractor implements AutoCloseable {
    private final MemorySegment handle;

    PlainTextExtractor(MemorySegment handle) {
        this.handle = handle;
    }

    MemorySegment handle() {
        return this.handle;
    }

    @Override
    public void close() {
        if (handle != null && !handle.equals(MemorySegment.NULL)) {
            try {
                NativeLib.KREUZBERG_PLAIN_TEXT_EXTRACTOR_FREE.invoke(handle);
            } catch (Throwable e) {
                throw new RuntimeException("Failed to free PlainTextExtractor: " + e.getMessage(), e);
            }
        }
    }
}
