// DO NOT EDIT - auto-generated by alef
package dev.kreuzberg;

import java.lang.foreign.MemorySegment;

/**
 * 7z archive extractor.
 * 
 * Extracts file lists and text content from 7z archives.
 */
public class SevenZExtractor implements AutoCloseable {
    private final MemorySegment handle;

    SevenZExtractor(MemorySegment handle) {
        this.handle = handle;
    }

    MemorySegment handle() {
        return this.handle;
    }

    @Override
    public void close() {
        if (handle != null && !handle.equals(MemorySegment.NULL)) {
            try {
                NativeLib.KREUZBERG_SEVEN_Z_EXTRACTOR_FREE.invoke(handle);
            } catch (Throwable e) {
                throw new RuntimeException("Failed to free SevenZExtractor: " + e.getMessage(), e);
            }
        }
    }
}
