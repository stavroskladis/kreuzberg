// DO NOT EDIT - auto-generated by alef
package dev.kreuzberg;

import java.lang.foreign.MemorySegment;

/**
 * Styled HTML renderer.
 *
 * Implements the [`Renderer`] trait; registered as `"html"` when the
 * `html` feature is active. Configuration is baked in at
 * construction time — no per-render allocation for CSS resolution.
 */
public class StyledHtmlRenderer implements AutoCloseable {
    private final MemorySegment handle;

    StyledHtmlRenderer(MemorySegment handle) {
        this.handle = handle;
    }

    MemorySegment handle() {
        return this.handle;
    }

    @Override
    public void close() {
        if (handle != null && !handle.equals(MemorySegment.NULL)) {
            try {
                NativeLib.KREUZBERG_STYLED_HTML_RENDERER_FREE.invoke(handle);
            } catch (Throwable e) {
                throw new RuntimeException("Failed to free StyledHtmlRenderer: " + e.getMessage(), e);
            }
        }
    }
}
