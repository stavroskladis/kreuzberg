// DO NOT EDIT - auto-generated by alef
package dev.kreuzberg;

import java.lang.foreign.MemorySegment;

/**
 * ZIP archive extractor.
 * 
 * Extracts file lists and text content from ZIP archives.
 */
public class ZipExtractor implements AutoCloseable {
    private final MemorySegment handle;

    ZipExtractor(MemorySegment handle) {
        this.handle = handle;
    }

    MemorySegment handle() {
        return this.handle;
    }

    @Override
    public void close() {
        if (handle != null && !handle.equals(MemorySegment.NULL)) {
            try {
                NativeLib.KREUZBERG_ZIP_EXTRACTOR_FREE.invoke(handle);
            } catch (Throwable e) {
                throw new RuntimeException("Failed to free ZipExtractor: " + e.getMessage(), e);
            }
        }
    }
}
