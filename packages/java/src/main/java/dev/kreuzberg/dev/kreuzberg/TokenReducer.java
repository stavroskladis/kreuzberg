// DO NOT EDIT - auto-generated by alef
package dev.kreuzberg;

import java.lang.foreign.MemorySegment;

public class TokenReducer implements AutoCloseable {
    private final MemorySegment handle;

    TokenReducer(MemorySegment handle) {
        this.handle = handle;
    }

    MemorySegment handle() {
        return this.handle;
    }

    @Override
    public void close() {
        if (handle != null && !handle.equals(MemorySegment.NULL)) {
            try {
                NativeLib.KREUZBERG_TOKEN_REDUCER_FREE.invoke(handle);
            } catch (Throwable e) {
                throw new RuntimeException("Failed to free TokenReducer: " + e.getMessage(), e);
            }
        }
    }
}
