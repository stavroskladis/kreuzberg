// DO NOT EDIT - auto-generated by alef
package dev.kreuzberg;

import java.lang.foreign.MemorySegment;

/**
 * Native Rust RTF extractor.
 * 
 * Extracts text content, metadata, and structure from RTF documents
 */
public class RtfExtractor implements AutoCloseable {
    private final MemorySegment handle;

    RtfExtractor(MemorySegment handle) {
        this.handle = handle;
    }

    MemorySegment handle() {
        return this.handle;
    }

    @Override
    public void close() {
        if (handle != null && !handle.equals(MemorySegment.NULL)) {
            try {
                NativeLib.KREUZBERG_RTF_EXTRACTOR_FREE.invoke(handle);
            } catch (Throwable e) {
                throw new RuntimeException("Failed to free RtfExtractor: " + e.getMessage(), e);
            }
        }
    }
}
