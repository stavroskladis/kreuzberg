// DO NOT EDIT - auto-generated by alef
package dev.kreuzberg;

import java.lang.foreign.MemorySegment;

/**
 * Registry for validator plugins.
 * 
 * Manages validators with priority-based execution order.
 */
public class ValidatorRegistry implements AutoCloseable {
    private final MemorySegment handle;

    ValidatorRegistry(MemorySegment handle) {
        this.handle = handle;
    }

    MemorySegment handle() {
        return this.handle;
    }

    @Override
    public void close() {
        if (handle != null && !handle.equals(MemorySegment.NULL)) {
            try {
                NativeLib.KREUZBERG_VALIDATOR_REGISTRY_FREE.invoke(handle);
            } catch (Throwable e) {
                throw new RuntimeException("Failed to free ValidatorRegistry: " + e.getMessage(), e);
            }
        }
    }
}
