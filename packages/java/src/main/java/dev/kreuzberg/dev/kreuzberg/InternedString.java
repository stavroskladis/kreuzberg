// DO NOT EDIT - auto-generated by alef
package dev.kreuzberg;

import java.lang.foreign.MemorySegment;

/**
 * A reference to an interned string stored in an Arc.
 * 
 * This wraps an Arc<String> and provides convenient access to the string content.
 * Multiple calls with the same string content will share the same Arc, reducing memory usage.
 */
public class InternedString implements AutoCloseable {
    private final MemorySegment handle;

    InternedString(MemorySegment handle) {
        this.handle = handle;
    }

    MemorySegment handle() {
        return this.handle;
    }

    @Override
    public void close() {
        if (handle != null && !handle.equals(MemorySegment.NULL)) {
            try {
                NativeLib.KREUZBERG_INTERNED_STRING_FREE.invoke(handle);
            } catch (Throwable e) {
                throw new RuntimeException("Failed to free InternedString: " + e.getMessage(), e);
            }
        }
    }
}
