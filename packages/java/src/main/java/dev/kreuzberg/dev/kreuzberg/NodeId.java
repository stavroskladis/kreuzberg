// DO NOT EDIT - auto-generated by alef
package dev.kreuzberg;

import java.lang.foreign.MemorySegment;

/**
 * Deterministic node identifier.
 *
 * Generated from a hash of `node_type + text + page`. The same document
 * always produces the same IDs, making them useful for diffing, caching,
 * and external references.
 */
public class NodeId implements AutoCloseable {
    private final MemorySegment handle;

    NodeId(MemorySegment handle) {
        this.handle = handle;
    }

    MemorySegment handle() {
        return this.handle;
    }

    @Override
    public void close() {
        if (handle != null && !handle.equals(MemorySegment.NULL)) {
            try {
                NativeLib.KREUZBERG_NODE_ID_FREE.invoke(handle);
            } catch (Throwable e) {
                throw new RuntimeException("Failed to free NodeId: " + e.getMessage(), e);
            }
        }
    }
}
