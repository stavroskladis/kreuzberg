// DO NOT EDIT - auto-generated by alef
package dev.kreuzberg;

import java.lang.foreign.MemorySegment;

/**
 * Djot markup extractor with metadata and table support.
 * 
 * Parses Djot documents with YAML frontmatter, extracting:
 * - Metadata from YAML frontmatter
 * - Plain text content
 * - Tables as structured data
 * - Document structure (headings, links, code blocks)
 */
public class DjotExtractor implements AutoCloseable {
    private final MemorySegment handle;

    DjotExtractor(MemorySegment handle) {
        this.handle = handle;
    }

    MemorySegment handle() {
        return this.handle;
    }

    @Override
    public void close() {
        if (handle != null && !handle.equals(MemorySegment.NULL)) {
            try {
                NativeLib.KREUZBERG_DJOT_EXTRACTOR_FREE.invoke(handle);
            } catch (Throwable e) {
                throw new RuntimeException("Failed to free DjotExtractor: " + e.getMessage(), e);
            }
        }
    }
}
