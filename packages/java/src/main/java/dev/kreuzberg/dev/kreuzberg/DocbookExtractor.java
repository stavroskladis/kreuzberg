// DO NOT EDIT - auto-generated by alef
package dev.kreuzberg;

import java.lang.foreign.MemorySegment;

/**
 * DocBook document extractor.
 * 
 * Supports both DocBook 4.x (no namespace) and 5.x (with namespace) formats.
 */
public class DocbookExtractor implements AutoCloseable {
    private final MemorySegment handle;

    DocbookExtractor(MemorySegment handle) {
        this.handle = handle;
    }

    MemorySegment handle() {
        return this.handle;
    }

    @Override
    public void close() {
        if (handle != null && !handle.equals(MemorySegment.NULL)) {
            try {
                NativeLib.KREUZBERG_DOCBOOK_EXTRACTOR_FREE.invoke(handle);
            } catch (Throwable e) {
                throw new RuntimeException("Failed to free DocbookExtractor: " + e.getMessage(), e);
            }
        }
    }
}
