// DO NOT EDIT - auto-generated by alef
package dev.kreuzberg;

import java.lang.foreign.MemorySegment;

/**
 * Trait for extractors that can work synchronously (WASM-compatible).
 * 
 * This trait defines the synchronous extraction interface for WASM targets and other
 * environments where async/tokio runtimes are not available or desirable.
 * 
 * # Implementation
 * 
 * Extractors that need to support WASM should implement this trait in addition to
 * the async `DocumentExtractor` trait. This allows the same extractor to work in both
 * environments by delegating to the sync implementation.
 * 
 * # MIME Type Validation
 * 
 * The `mime_type` parameter is guaranteed to be already validated.
 * 
 * # Example
 * 
 * ```rust,ignore
 * impl SyncExtractor for PlainTextExtractor {
 *     fn extract_sync(&self, content: &[u8], config: &ExtractionConfig) -> Result<ExtractionResult> {
 *         let text = String::from_utf8_lossy(content).to_string();
 *         Ok(ExtractionResult {
 *             content: text,
 *             mime_type: "text/plain".to_string(),
 *             metadata: Metadata::default(),
 *             tables: vec![],
 *             detected_languages: None,
 *             chunks: None,
 *             images: None,
 *         })
 *     }
 * }
 * ```
 */
public class SyncExtractor implements AutoCloseable {
    private final MemorySegment handle;

    SyncExtractor(MemorySegment handle) {
        this.handle = handle;
    }

    MemorySegment handle() {
        return this.handle;
    }

    @Override
    public void close() {
        if (handle != null && !handle.equals(MemorySegment.NULL)) {
            try {
                NativeLib.KREUZBERG_SYNC_EXTRACTOR_FREE.invoke(handle);
            } catch (Throwable e) {
                throw new RuntimeException("Failed to free SyncExtractor: " + e.getMessage(), e);
            }
        }
    }
}
