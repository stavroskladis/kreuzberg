// DO NOT EDIT - auto-generated by alef
package dev.kreuzberg;

import java.lang.foreign.MemorySegment;

/**
 * Citation format extractor for RIS, PubMed/MEDLINE, and EndNote XML formats.
 * 
 * Parses citation files and extracts structured bibliography data including
 * entries, authors, publication years, and format-specific metadata.
 */
public class CitationExtractor implements AutoCloseable {
    private final MemorySegment handle;

    CitationExtractor(MemorySegment handle) {
        this.handle = handle;
    }

    MemorySegment handle() {
        return this.handle;
    }

    @Override
    public void close() {
        if (handle != null && !handle.equals(MemorySegment.NULL)) {
            try {
                NativeLib.KREUZBERG_CITATION_EXTRACTOR_FREE.invoke(handle);
            } catch (Throwable e) {
                throw new RuntimeException("Failed to free CitationExtractor: " + e.getMessage(), e);
            }
        }
    }
}
