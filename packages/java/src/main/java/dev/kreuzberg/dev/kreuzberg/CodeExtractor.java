// DO NOT EDIT - auto-generated by alef
package dev.kreuzberg;

import java.lang.foreign.MemorySegment;

/**
 * Source code extractor using tree-sitter language pack.
 * 
 * Detects the programming language from the file extension or shebang line,
 * then uses tree-sitter to parse and extract structural information.
 */
public class CodeExtractor implements AutoCloseable {
    private final MemorySegment handle;

    CodeExtractor(MemorySegment handle) {
        this.handle = handle;
    }

    MemorySegment handle() {
        return this.handle;
    }

    @Override
    public void close() {
        if (handle != null && !handle.equals(MemorySegment.NULL)) {
            try {
                NativeLib.KREUZBERG_CODE_EXTRACTOR_FREE.invoke(handle);
            } catch (Throwable e) {
                throw new RuntimeException("Failed to free CodeExtractor: " + e.getMessage(), e);
            }
        }
    }
}
