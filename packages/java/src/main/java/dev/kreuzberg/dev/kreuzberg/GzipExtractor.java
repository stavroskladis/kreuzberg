// DO NOT EDIT - auto-generated by alef
package dev.kreuzberg;

import java.lang.foreign.MemorySegment;

/**
 * Gzip archive extractor.
 * 
 * Decompresses gzip files and extracts text content from the compressed data.
 */
public class GzipExtractor implements AutoCloseable {
    private final MemorySegment handle;

    GzipExtractor(MemorySegment handle) {
        this.handle = handle;
    }

    MemorySegment handle() {
        return this.handle;
    }

    @Override
    public void close() {
        if (handle != null && !handle.equals(MemorySegment.NULL)) {
            try {
                NativeLib.KREUZBERG_GZIP_EXTRACTOR_FREE.invoke(handle);
            } catch (Throwable e) {
                throw new RuntimeException("Failed to free GzipExtractor: " + e.getMessage(), e);
            }
        }
    }
}
