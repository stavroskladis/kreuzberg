// DO NOT EDIT - auto-generated by alef
package dev.kreuzberg;

import java.lang.foreign.MemorySegment;

/**
 * Apple Pages document extractor.
 * 
 * Supports `.pages` files (modern iWork format, 2013+).
 * 
 * Extracts all text content from the document by parsing the IWA
 * (iWork Archive) container: ZIP → Snappy → protobuf text fields.
 */
public class PagesExtractor implements AutoCloseable {
    private final MemorySegment handle;

    PagesExtractor(MemorySegment handle) {
        this.handle = handle;
    }

    MemorySegment handle() {
        return this.handle;
    }

    @Override
    public void close() {
        if (handle != null && !handle.equals(MemorySegment.NULL)) {
            try {
                NativeLib.KREUZBERG_PAGES_EXTRACTOR_FREE.invoke(handle);
            } catch (Throwable e) {
                throw new RuntimeException("Failed to free PagesExtractor: " + e.getMessage(), e);
            }
        }
    }
}
