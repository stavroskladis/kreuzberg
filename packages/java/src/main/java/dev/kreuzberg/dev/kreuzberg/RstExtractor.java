// DO NOT EDIT - auto-generated by alef
package dev.kreuzberg;

import java.lang.foreign.MemorySegment;

/**
 * Native Rust reStructuredText extractor.
 * 
 * Parses RST documents using document tree parsing and extracts:
 * - Metadata from field lists
 * - Document structure (headings, sections)
 * - Text content and inline formatting
 * - Code blocks and directives
 * - Tables and lists
 */
public class RstExtractor implements AutoCloseable {
    private final MemorySegment handle;

    RstExtractor(MemorySegment handle) {
        this.handle = handle;
    }

    MemorySegment handle() {
        return this.handle;
    }

    @Override
    public void close() {
        if (handle != null && !handle.equals(MemorySegment.NULL)) {
            try {
                NativeLib.KREUZBERG_RST_EXTRACTOR_FREE.invoke(handle);
            } catch (Throwable e) {
                throw new RuntimeException("Failed to free RstExtractor: " + e.getMessage(), e);
            }
        }
    }
}
