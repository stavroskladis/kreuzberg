// DO NOT EDIT - auto-generated by alef
package dev.kreuzberg;

import java.lang.foreign.MemorySegment;

/**
 * Markdown extractor with metadata and table support.
 * 
 * Parses markdown documents with YAML frontmatter, extracting:
 * - Metadata from YAML frontmatter
 * - Plain text content
 * - Tables as structured data
 * - Document structure (headings, links, code blocks)
 * - Images from data URIs
 */
public class MarkdownExtractor implements AutoCloseable {
    private final MemorySegment handle;

    MarkdownExtractor(MemorySegment handle) {
        this.handle = handle;
    }

    MemorySegment handle() {
        return this.handle;
    }

    @Override
    public void close() {
        if (handle != null && !handle.equals(MemorySegment.NULL)) {
            try {
                NativeLib.KREUZBERG_MARKDOWN_EXTRACTOR_FREE.invoke(handle);
            } catch (Throwable e) {
                throw new RuntimeException("Failed to free MarkdownExtractor: " + e.getMessage(), e);
            }
        }
    }
}
