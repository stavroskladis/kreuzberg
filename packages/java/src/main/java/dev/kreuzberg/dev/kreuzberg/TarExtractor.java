// DO NOT EDIT - auto-generated by alef
package dev.kreuzberg;

import java.lang.foreign.MemorySegment;

/**
 * TAR archive extractor.
 * 
 * Extracts file lists and text content from TAR archives.
 */
public class TarExtractor implements AutoCloseable {
    private final MemorySegment handle;

    TarExtractor(MemorySegment handle) {
        this.handle = handle;
    }

    MemorySegment handle() {
        return this.handle;
    }

    @Override
    public void close() {
        if (handle != null && !handle.equals(MemorySegment.NULL)) {
            try {
                NativeLib.KREUZBERG_TAR_EXTRACTOR_FREE.invoke(handle);
            } catch (Throwable e) {
                throw new RuntimeException("Failed to free TarExtractor: " + e.getMessage(), e);
            }
        }
    }
}
