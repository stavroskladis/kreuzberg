// DO NOT EDIT - auto-generated by alef
package dev.kreuzberg;

import java.lang.foreign.MemorySegment;

/**
 * Email message extractor.
 * 
 * Supports: .eml, .msg
 */
public class EmailExtractor implements AutoCloseable {
    private final MemorySegment handle;

    EmailExtractor(MemorySegment handle) {
        this.handle = handle;
    }

    MemorySegment handle() {
        return this.handle;
    }

    @Override
    public void close() {
        if (handle != null && !handle.equals(MemorySegment.NULL)) {
            try {
                NativeLib.KREUZBERG_EMAIL_EXTRACTOR_FREE.invoke(handle);
            } catch (Throwable e) {
                throw new RuntimeException("Failed to free EmailExtractor: " + e.getMessage(), e);
            }
        }
    }
}
